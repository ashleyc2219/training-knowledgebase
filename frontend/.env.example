# Copy this file to .env and fill in your key to enable real Claude-generated
# tutorial content, refinement rewrites, etc. Without it, the app falls back
# to plain templates — the demo still works, just with less polished text.
# The key is read server-side only (see vite.config.ts) and is never sent
# to the browser.
ANTHROPIC_API_KEY=
