# 來源：../draft/training-kb-design-doc.md §8.5、§11.2、§12.1、§15、§16。
# 來源：../draft/training-kb-erd.md §5。
Feature: 驗證教學規則
  Analytics 對教學規則執行成效驗證，根據套用與未套用版本的結果更新狀態。

  # 來源：設計 §11.2、§15；釐清 F31。
  @Missing
  Rule: 規則成效比較套用組與同一篇教學套用前的已發布版本
    # 對照組是同一篇教學、套用前的版本，採前後比較。
    # published_at 為空的未發布版本不納入對照。
    #TODO 原文沒有完整版本集合與前後對照的評分計算紀錄。

  # 來源：設計 §11.2、§12.1 第 4 輪；釐清 F30。
  @Missing
  Rule: candidate 僅在平均評分嚴格提高且重開票率嚴格下降時升為 active
    # 兩項都必須成立才啟用；只改善一項或持平不得升級。
    # Demo 的 7 與 2 是重開票筆數，不能當成已驗證比例。
    #TODO 原文提供評分 4.4 vs 2.9、重開票數 2 vs 7，沒有可重算的原始評分集合。

  # 來源：設計 §11.2；釐清 F32。
  @Missing
  Rule: 未載入完整核定種子驗證批次時不改變規則狀態
    # 只在完整核定種子驗證批次載入後才改規則狀態。
    # 線上資料不足維持原狀態；candidate 繼續為 candidate。
    # 線上不足不等於無效，不得因此改為 retired。
    #TODO 原文未提供種子驗證批次清單，也沒有線上資料不足時的狀態紀錄。

  # 來源：設計 §11.2。
  @Missing
  Rule: 與既有規則衝突的 candidate 規則變為 retired
    #TODO 原文只有請寫詳細一點與太長的衝突描述，沒有具名規則、範圍與裁決結果。
    # 由模型輸出結構化衝突判定與證據；Analytics 依通過驗證的判定自動把該 candidate 寫成 retired。
    # 不自動覆蓋或退役既有 active 規則。

  # 來源：設計 §11.2、§12.1 第 5 輪。
  @Missing
  Rule: 後來失效的 active 規則變為 retired
    #TODO R-012 的前置狀態未明列，不能補成 active → retired 的完整狀態轉換案例。
    # 連續兩個不重疊的成熟評估窗口都未達 F30 改善判準才退役；單一窗口未改善不退役。
    # 成熟評估窗口對齊 F32：完整核定種子驗證批次。線上不足維持原狀態。

  # 來源：設計 §11.2、§12.1、§16；釐清 F33。
  @Partial
  Rule: 驗證無效的規則從可使用規則中退役
    # 連續兩個不重疊的核定種子驗證批次都未達 F30 改善判準才退役。
    # 持平視為未改善。線上資料不足不得冒充無效。
    #TODO 原文沒有連續兩個批次的前態與指標集合；此例只保留原文明示的已判定結果。

    Example: R-012 增加缺少資訊回饋後退役
      Given 規則資料如下
        | rule_id | rule             |
        | R-012   | 每步不超過兩句   |
      And "R-012" 的成效驗證結果為 "無效反增缺少資訊"
      When Analytics 更新規則 "R-012" 的驗證結果
      Then 規則 "R-012" 的 status 為 "retired"

  # 來源：設計 §11.2；釐清 F34。
  @Missing
  Rule: curation 合併相近教學規則
    #TODO 原文未定義觸發時機、相近判準、合併後 ID、evidence 或 applies_when 的資料結果。
    # MVP curation 只建立相近群組供人工檢視。
    # 不自動改寫規則、不移轉 ID／證據／歷史、不自動改 status。

  # 來源：設計 §8.5；ERD §5。
  @Missing
  Rule: Analytics 寫入教學規則的驗證後 status
    #TODO 原文提供讀寫矩陣，沒有含寫入主體的執行紀錄。
