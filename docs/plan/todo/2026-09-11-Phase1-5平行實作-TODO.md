# 2026-09-11 — Phase 1～5 平行實作 TODO

> ⚠️ 這只是 hackathon 作品，不要過度設計。先能跑、能 demo，再談漂亮。

- **依據：** `docs/plan/unfinish/00-總覽.md` ～ `06-Phase5-*.md`（七份計畫）、`docs/design/showme.md`
- **開發法：** TDD + BDD——每個模組先依 `docs/spec/features/*.feature` 的 Example 寫 pytest（Given 表 → When → Then 表），再寫功能碼；全部測試要綠。
- **狀態：** Phase 0 已完成（35 tests 綠、Bitext 真下載、Streamlit 空殼可啟動）。本 TODO 涵蓋 Phase 1～5。

## 實作邏輯

1. **檔案所有權切分，五個 opus subagent 平行做**（共用契約見總覽 §6）：

   | Agent | 擁有的檔案 | 對齊 .feature |
   |---|---|---|
   | P1 種子建圖 | `app/memory/cognee_client.py`、`app/memory/hydradb_client.py`、`app/ingest/seed.py`、`app/analytics/hotdata_client.py`、`tests/integration/test_seed.py` | 建構知識圖譜（5） |
   | P2 即時路徑＋CREATE | `app/agent/realtime.py`、`analysis.py`、`create_tutorial.py`、`llm.py`、`rocketride_client.py`、`pipeline.json`、`app/ingest/poll.py`、`app/demo/ui_tickets.py`、`tests/unit/test_realtime.py`／`test_analysis.py`／`test_create.py` | 輪詢新票單（1）、自動回覆顧客（5）、分析SupportTickets（9）、建立Tutorial（5） |
   | P3 Rote 重放 | `app/muscle/rote_client.py`、`app/api/deflect_api.py`、`tests/unit/test_rote_client.py`、rote adapter／Play | 重放已驗證流程（3） |
   | P4 Feedback＋REFINE＋指標 | `app/ingest/feedback.py`、`app/agent/feedback_review.py`、`app/analytics/metrics.py`（snapshot）、`app/demo/ui_feedback.py`、`app/demo/ui_metrics.py`、`tests/unit/test_feedback.py`／`test_review.py` | 收集Feedback（7）、定期優化Tutorial（10）、展示學習指標（3） |
   | P5 Release＋Snyk＋彩排 | `app/agent/release_update.py`、`app/ingest/changelog.py`、`app/demo/ui_release.py`、`scripts/demo_rehearsal.py`、`tests/unit/test_release.py`、Snyk 安裝與掃描 | 輪詢ReleaseNote（3）、依ReleaseNote更新Tutorial（7） |

2. **共用介面先放 stub，避免互相等待：**
   - `app/agent/llm.py`：`complete_json(prompt, schema_hint) -> dict`，provider 鏈 RocketRide → Anthropic API → `LLMUnavailable`；呼叫端各自有模板降級。
   - `app/muscle/rote_client.py`：`on_deflected(db, ticket_id, user_problem_id) -> dict`，即時路徑在 deflected 成功後呼叫；P3 換成真 Rote 實作。
   - Streamlit 各區：`app/demo/ui_<x>.py` 提供 `render(db)`；最後由整合步驟接進 `streamlit_app.py`。
3. **外部服務現況與降級：** hotdata 已登入（建 `support` database）；rote 已登入；Cognee 伺服器可用（沿用本機記憶插件的 server，dataset 另開）；HydraDB 無直連憑證 → `hydradb_client` 先用本機圖譜檔（`.state/graph.json`）保留五種邊與多跳查詢介面，🖐️ 拿到連線字串後只換 transport；無 LLM 金鑰 → 模板成型五欄並標「模板」。
4. **整合與驗收：** 五個 agent 回報後接 Streamlit、跑全部測試、跑 `scripts/demo_rehearsal.py`（sqlite 模式）走完六步、寫 report、把 `unfinish/` 搬到 `finish/`。

## 步驟

- [x] Phase 0 骨架（已完成，見 `docs/plan/unfinish/01-Phase0-骨架與環境.md`）
- [x] 放共用 stub：`app/agent/llm.py`、`app/muscle/rote_client.py` 介面
- [x] 各計畫檔加「現況更新」段（Phase 0 完成、環境事實）
- [x] 平行派 P1～P5（opus）
- [x] 整合 Streamlit 三區（`app/demo/streamlit_app.py` 接 ui_tickets / ui_release / ui_feedback / ui_metrics，側欄一鍵重置）
- [x] 全部測試綠：`uv run pytest -q -m "not integration"` 190 passed（2026-09-11 14:45）
- [x] 彩排腳本走完六步（`scripts/demo_rehearsal.py` 8 步 5.7s，含真 Rote 重放）
- [x] 寫 report（`docs/plan/report/2026-09-11-Phase0..5-*-REP.md` 六份＋`全部Phase整合-REP.md`）；`unfinish/` 七份計畫已搬到 `finish/`
- [ ] 🖐️ 留給使用者：HydraDB 連線字串、LLM 金鑰（Anthropic 或 RocketRide 上的 Bedrock 憑證）、RocketRide deploy（只用 `ROCKETRIDE_DEPLOY_*`）、`snyk auth`
