# 2026-09-14 Phase 01–20 實作 TODO

> 對應計畫：`docs/plan/unfinish/00-總覽.md`、`00A`、`00B` 與 `01-Phase01` … `20-Phase20`。
> 任務來源：`docs/plan/dev-prompts/phase0914-0.md`。本檔記錄「打算怎麼做、做到哪裡」；做完的細節寫在 `docs/plan/report/`。

## 一、實作邏輯（為什麼這樣排）

1. **計畫文件已於 2026-09-14 完成 63 份審閱（見 report `2026-09-14-實作計畫產生與審閱-REP.md`）**，且 repo 目前沒有任何應用程式碼，與計畫假設一致。因此「更新計畫檔案」採**逐 Phase 邊做邊修**：實作者發現 Phase 文件與 00A 或與已寫好的程式衝突時，依 00 總覽規則「改 Phase 文件」並勾選完成清單；00／00A／00B 只在裁決改變時才更新。
2. **一律 TDD／BDD**：每個 Task 先寫會失敗的測試（紅燈）→ 最小實作 → 綠燈 → 只提交自己的檔案。BDD 的「行為」來自 `docs/spec/features/*.feature` 的 Rule，由 00B 指派給各 Phase 的 primary Rule 轉成可執行斷言。
3. **能平行就平行**：依 00A §3.2 的檔案 owner 表，只讓「不共用檔案」的 Phase 同時進行；共用 `models.py`、`repository.py`、`writing/client.py` 的 Phase 一律串行。
4. **相依一次加齊**：Phase 01 就把 boto3、moto、boto3-stubs、aws-cdk-lib、constructs 寫進 `pyproject.toml`，避免平行 Phase 同時改 `uv.lock` 互相衝突。
5. **只 review 重要的**：P05（鍵契約）、P06（Repository）、P10／P11（O2 操作紀錄）、P20（版號）做獨立 review；其餘靠自我檢查與最後整支 branch 的總 review。
6. **真實 AWS gate 不假稱通過**：O2／O3／O5 需要真實帳號證據（P11／P12／P14），程式綠燈不等於 gate PASS；沒證據就在報告標 FAIL／BLOCKED。

## 二、階段與步驟（波次）

| 波次 | 同時進行的 Phase | 依賴理由 |
|---|---|---|
| W0 | P01 專案骨架 | 全部的地基（uv、pytest、ruff、mypy、aws marker） |
| W1 | P02 設定／時間／錯誤 ∥ P03 列舉與草稿模型 | 各自獨立檔案 |
| W2 | P04 十個實體 ∥ P14 O5 模型探測 | P04 接 P03 的 `models.py`；P14 只需 P02 的 Settings |
| W3 | P05 單表鍵 ∥ P13 O6 來源 ID ∥ P15 Writer 介面 | P05 接 P04；P13 接 P04；P15 需 P14 的 O5 證據 |
| W4 | P06 Repository metadata ∥ P09 CDK 資料資源 ∥ P16 Titan embedding | P06 接 P05；P09 接 P05 常數；P16 接 P15 |
| W5 | P07 S3 與關係邊 ∥ P17 Claude schema／prompt | 各接 P06／P16 |
| W6 | P08 分頁查詢 ∥ P18 業務驗證與重試 | 各接 P07／P17 |
| W7 | P10 O2 操作紀錄 ∥ P12 O3 spike ∥ P19 規則選取 | 都只需 P08 完成 |
| W8 | P11 O2 整合驗證 ∥ P20 版號分配 | P11 接 P10；P20 接 P10＋P19 |

每個 Phase 的固定步驟：

1. 讀該 Phase 文件、00A 相關節、00B 的 primary Rule。
2. 依 Task 順序：寫測試 → `uv run pytest` 看紅燈 → 最小實作 → 綠燈 → `uv run ruff check src tests`、`uv run mypy src` → 只 `git add` 自己的檔案後 commit。
3. 勾選 Phase 文件的 checkbox 與完成清單；文件與程式不一致就修文件。
4. 寫實作報告（`.superpowers/sdd/phase0914-0/phaseNN-report.md`），最後彙整到 `docs/plan/report/`。

## 三、進度勾選

- [x] W0 P01（controller 親自實作；`5b14447`..`581c5ac`）
- [x] W1 P02、P03
- [x] W2 P04、P14（P14 的 **O5 = BLOCKED**，帳號未開通 Bedrock）
- [x] W3 P05、P13、P15（P05 review 一輪修完；O1 暫定接受、O6 待維護者核定）
- [x] W4 P06、P09、P16（P06 review 一個 Important 由 P07 帶修；P09 已實際部署 us-east-1）
- [x] W5 P07、P17
- [x] W6 P08、P18
- [x] W7 P10、P12、P19（P10 review 三個 Important 由 P11 帶修；P12 的 **O3 = FAIL**）
- [x] W8 P11、P20（P11 的 **O2 = PASS**；P20 的 review 併入最終整支 branch review）
- [ ] 最終整支 branch review 與修正（controller 進行中，完成後由 controller 勾選）
- [x] 00／00A／00B 與 20 份 Phase 文件狀態核對（各 Phase 文件由實作者邊做邊勾；00／00A／00B 的 carry-forward 與 Phase 01 文件由文件整合者於 2026-09-14 同步）
- [ ] `docs/plan/report/2026-09-14-Phase01-20實作-REP.md`（初稿已寫，第 6 節「測試結果」待 controller 於最終 review 後填入最終數字並勾選）
