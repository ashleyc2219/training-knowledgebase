# SDD ledger — plan: docs/plan/dev-prompts/phase0914-2.md

Scope: Phase 41–60 (docs/plan/unfinish). Branch backend, in-place. BASE at batch start: 9bebda6 (MERGE_BASE for final review).
Baseline 2026-09-14 20:05: `uv run pytest tests -q -W error` → 924 passed / 23 skipped / 11 xfailed. ruff/mypy clean.
Shared rules: `.superpowers/sdd/phase0914-2/COMMON.md`.

## Rulings (preflight)
- Ruling: user overrides skill defaults — parallel implementers allowed within a wave (opus), reviews only for P41, P42, P48, P52, P59 + final whole-branch review; no per-task review elsewhere — cost if wrong: an unreviewed phase carries a defect to final review.
- Ruling: Step 1 of the prompt (update all 20 Phase docs first) is done as wave W0 by 7 doc-update agents grouped by chain; each also writes an implementation brief per Phase — cost if wrong: ~7 agent runs of doc work before code starts.
- Ruling: real-AWS hookup happens in this batch (R1). O5 still BLOCKED (re-probe 20:0x, report o5-20260915T030245Z.md): Bedrock-dependent paths on real AWS are recorded as BLOCKED with the actual error, never as pass — cost if wrong: evidence must be re-collected once model access is granted.
- Ruling: O3 stays FAIL; P48/P52/P57/P59 implement protocol A as P24/P25 did and record observed cut-point behaviour; F49 never relaxed — cost if wrong: publish code shape changes if maintainer picks a different exit (structure is exit-agnostic).
- Ruling: Lambda packaging via a reproducible deps layer/bundling owned by P41; later stack phases reuse it (R2) — cost if wrong: redeploy.
- Ruling: same-file concurrency allowed within a wave under R3 (Edit-only, own sections, add-by-path) — cost if wrong: a mixed commit or a transient red in someone else's in-progress test file.
- Ruling: controller pre-creates docstring-only skeletons `pipelines/feedback.py`, `pipelines/release.py` (R4) so parallel phases only Edit — cost if wrong: none (TDD red still comes from missing names).
- Ruling: phase reports go straight to `docs/plan/report/phases/2026-09-14-Phase<nn>-REP.md` and are committed with the doc update (R7) — cost if wrong: none.

## Preflight conflict scan (file sharing from 00A §3.2)
| chain | shared file | resolution |
|---|---|---|
| P41→P42→P48/P52→P54 | infra/training_kb_stack.py | P41 creates; P42∥P54 add Lambdas (Edit-only, own sections); P48∥P52 add state machines (Edit-only) |
| P41 | pipelines/common.py, pipelines/ticket.py | only P41 this batch |
| P42→P43→P59 | ingress.py | sequential: P42 (W2) → P43 (W3) → P59 (W4) |
| P44∥P45∥P47 → P46 → P48 | pipelines/feedback.py | W1: P44/P45/P47 own functions; W2: P46 (needs P45 DiagnosisResult); W3: P48 handler |
| P45, P46, P47, P50, P51, P43 | writing/prompts.py | append-only own sections (Edit-only) |
| P47 | writing/schemas.py | only P47 |
| P44 | config.py | only P44 |
| P49∥P50 → P51 → P52 | pipelines/release.py | W1: P49/P50; W2: P51 (needs P50 hits); W3: P52 |
| P53 ∥ P54 → P55 | analytics/* | P53 ratings.py+__init__; P54 reopen/rules_metrics/version + handlers/analytics.py; P55 validation + status_writer write side + handler action + tests/unit/conftest.py |
| P57 → P59 | publishing.py (site_diff_key by P57; maybe_fail/resume_publish by P59) | P57 (W1/W2) before P59 (W4) |
| P57 | site.py, infra/training_kb_data_stack.py, demo/site_assets | only P57 |
| P56, P58 | demo/* | distinct files; P58 after P56 (seed) and P54 (metrics) |
| P59 | faults.py, content.py, publishing.py, ingress.py, infra/scripts/check_asl.py | after P41, P48, P52 |
| P60 | infra/scripts/checks.py | last |

## Wave plan
W0 doc-update (7 agents): G1 P41 | G2 P42,P43 | G3 P44–P48 | G4 P49–P52 | G5 P53–P55 | G6 P56–P58 | G7 P59,P60
W1: P41 ∥ P44 ∥ P45 ∥ P47 ∥ P49 ∥ P50 ∥ P53 ∥ P56 ∥ P57
W2: P42 ∥ P46 ∥ P51 ∥ P54
W3: P43 ∥ P48 ∥ P52 ∥ P55 ∥ P58
W4: P59
W5: P60 → final whole-branch review → fix wave → 00/00A/00B sync → REP → TODO tick.

## Progress
- 5f8a430: TODO + skeletons committed. W0 dispatched (opus ×7): G1=a2351ad80c302802c, G2=a8a6aaa402eda5dbb, G3=a37bc83b30a3fd540, G4=acf320b079056e6b1, G5=ab73645cceeab52d2, G6=a24bcd4ca4cec630b, G7=afcdc9407f11753f8. IMPLEMENTER.md written for W1+.
- W0-G1 (P41): complete DONE_WITH_CONCERNS (commit e861b39; brief briefs/P41-brief.md). Finding: ensure_embedding skips Bedrock when embedding already stored → real-AWS SUCCEEDED path exists under O5 BLOCKED (seed ticket with embedding, cluster<5 → NotRecurring). Layer recipe verified: `uv pip install --target build/lambda-layer/python --python-platform x86_64-manylinux2014 --python-version 3.12 --only-binary=:all: pydantic jsonschema`.
  - Ruling: accept new env `TKB_FAULT_TASK`; P41 builds faults.py first slice (five 00A points + public names, no maybe_fail inserted), P59 extends; 00A §3.5/§3.2 + P60 deploy checklist updated at closing doc sync — cost if wrong: one env var to remove.
  - Ruling: ASL ErrorEquals matches class-name string only (no inheritance) → fault injection must raise `TransientError`/`PermanentError` themselves, never a subclass; carry to P59 dispatch — cost if wrong: retries silently never fire on real AWS.
  - Ruling: D-79 "SK begins_with APPLIED_TO#" IAM condition is impossible (DynamoDB IAM has no SK condition key) → grant DeleteItem on the single table + code-level DELETABLE_RELATIONS guard; revise 00A D-79 at doc sync — cost if wrong: broader IAM than spec text.
  - Ruling: `TrainingKbStack(scope, id, *, data: TrainingKbDataStack)` cross-stack signature; P42/P48/P52/P54 reuse; 00A §3.2/D-43 note at doc sync — cost if wrong: constructor churn.
  - Ruling: A8 (P24/P25 website endpoint acceptance) routed to P57 (hosting) / P59 (cut-point replay) — cost if wrong: none.
- W0-G2 (P42, P43): complete DONE_WITH_CONCERNS (commits fd9a5f9, bc28a42; briefs P42/P43). Rulings accepted from G2: O6 does not block P42 feedback/view path (fixed import never builds RawEvent); `validate_feedback`/`validate_view` reuse `stable_user_from_import`; multi-item fan-out lives in handlers/import_.py (adapters `_batch_item` untouched).
  - Ruling: 00A line ~915 wording ("validate_feedback 必須呼叫 assert_accepts_feedback") is wrong; call site is `import_feedback` (00A line 425 is authoritative) → fix at closing doc sync — cost if wrong: none.
  - Ruling: P52 must `grant_start_execution(import_fn)` on training-kb-release-update for the import Lambda's release branch (carry into P52 dispatch) — cost if wrong: release import fails with AccessDenied in cloud.
  - Note: W3 has P43∥P48∥P52 all editing infra/training_kb_stack.py — Edit-only/own sections; controller verifies synth after W3.
- W0-G5 (P53–P55): complete DONE_WITH_CONCERNS (commits c1f7a5e, 6ccc62c, 1670dc4, 0f7137c; briefs P53/P54/P55). Accepted: analytics/__init__.py stays docstring-only (no re-export); P55 doc bug fixed (pytest.approx).
  - Ruling: P54 stays in W2 (concurrent with P42, after P41); its stack Template assertion uses the documented inclusion-style fallback (not the four-name equality) because P42's import Lambda lands in the same wave — cost if wrong: one assertion weaker than the doc's ideal; final review may tighten.
  - Ruling: P54 does NOT edit P48's test file (P48 runs later in W3, file does not exist yet); P48 writes `test_feedback_review_flow.py` with the final four Lambda names itself — carry into P48 dispatch — cost if wrong: none.
  - Ruling: 00B §2.12 VAL Rule 3 test name mismatch → P55 uses the Phase doc's name `test_overlapping_undecidable_or_unapproved_batches_never_retire`; 00B fixed at closing doc sync — cost if wrong: none.
- W0-G7 (P59, P60): complete DONE_WITH_CONCERNS (commits d2a2bd8, bd6be7d; briefs P59/P60). Accepted: cut-point 4 raises PublishError (not TransientError) per P24 `_after_transaction` — test expects `(TransientError, PublishError)` + message contains cut-point code; `resume_publish` uses `publishing.public_site_keys(...)`; P60 `check_iam` scans only IAM resources' Allow statements over both stack templates; `check_secrets` scope = `git ls-files` minus docs/ and .superpowers/; Snyk CLI present but unauthenticated → `not_run` + pip-audit alternative labelled "非 Snyk".
  - Ruling: P41 doc §4 lacks faults.py but G1 brief §7 has P41 building its first slice; P59 extends — cost if wrong: none.
  - Note for maintainer (closing REP): `.env.example` lacks `TKB_GITHUB_WEBHOOK_SECRET` line; Snyk auth needed for a real scan.
- W0-G4 (P49–P52): complete DONE_WITH_CONCERNS (commits 8ecce63, a177a38, 16c59b1, 4a0f0ac; briefs P49–P52). Accepted: local test fixtures per file (RecordingWriter incompatible); D-83 as P52 Task 1 Step 3b (`Publisher._write_tutorial_index(slug)`); P52 matrix split into 可實證 / BLOCKED.
  - Ruling: `pipeline_task_handler` dispatches ONE task: select pipeline by event["pipeline"] → `<pipeline>_handler(event, context)` → `_TASK_BY_NAME[event["task"]]`; `run_sequence` stays the local full-sequence runner (`run_*`) for tests. 00A §7 diagram wording fixed at doc sync. Carry into P41 dispatch — cost if wrong: each Task state would re-run the whole pipeline.
  - Ruling: `LEASE_TTL_SECONDS = 120` pre-declared by controller in feedback.py (commit below) so P51 (W2) can import while P46 (W2) works; owner stays P46 — cost if wrong: none.
  - Ruling: app stack id is `TrainingKbApp` (P41 doc D-43); carry into P52/P48/P42/P54 dispatches — cost if wrong: none.
  - Note: subagent commit trailers say "Claude Opus 5 (1M context)" (their harness attribution) — accepted, no rewrite.
- W0-G3 (P44–P48): complete DONE_WITH_CONCERNS (commits 4d22b18, b3ddac9, b825171, d9c9a83, c8701d5; briefs P44–P48). Accepted: P44 does not need config.py (weak_average already exists — 00A §6.9 row stale, fix at doc sync); P47 schemas.py is a no-op; O2 PASS wording fixed in P46/P47/P48; P45 fixture renamed `diagnosis_writer`; P48 test uses `>=` on Lambda names if P42 not landed (but P42 is W2, P48 is W3 → equality with four names incl. analytics per G5 ruling).
  - Ruling: 00A §4.2 O5 row → point to o5-20260915T030245Z.md at doc sync — cost if wrong: none.
- W0-G6 (P56–P58): complete DONE_WITH_CONCERNS (commits c44428b, fde3f65, 137352f; briefs P56–P58). Accepted: P57 uses same-directory relative hrefs (D-78 lock; no import cycle); `site_diff_key` lives in publishing.py (D-54; 00A §3.2 line ~132 fixed at doc sync); P57 owns the 4 affected assertions in tests/unit/test_data_stack.py; data-role `site/` write grant → P57 (data stack), Lambda-role → P41; P58 adds streamlit + uv.lock.
  - Ruling: P56 moves to W4 (needs P54 `reopen_stats`, P55 `SeedBatch`, P43 constants) and P58 to W5 (needs P56 seed + P54 metrics); P60 to W6. Demo chain is off the AWS-critical path — cost if wrong: ~1 extra wave of wall-clock.
  - Ruling: `DEFAULT_FEEDBACK_CATEGORIES` pre-declared by controller in ingress.py (owner P43) — cost if wrong: none.
  - Ruling: `demo/` importability → P56 adds `[tool.pytest.ini_options] pythonpath = ["."]`, mypy `files` += "demo", ruff paths += demo; COMMON.md lint lines updated then — cost if wrong: pytest config churn.
  - Ruling: P57 leaves the widget→import half of `test_site_widget_roundtrip.py` to P42 (W2) as a documented R3.6 exception — carry into P42 dispatch — cost if wrong: none.
  - Note for P60: `check_public` must accept relative hrefs.
- ALL W0 COMPLETE (20 docs updated, 20 briefs). Revised waves: W1 P41∥P44∥P45∥P47∥P49∥P50∥P53∥P57 | W2 P42∥P46∥P51∥P54 | W3 P43∥P48∥P52∥P55 | W4 P56∥P59 | W5 P58 | W6 P60 → final review → fix wave → doc sync → REP.
- Ruling: gate excludes `ruff format` (pre-existing files are not ruff-formatted; batch 1 used `ruff check` only); COMMON.md/IMPLEMENTER.md patched — cost if wrong: mixed formatting in new files.
- W1 dispatched at BASE b3a2de2 (opus ×8): P41=ad0e7a23a531d9ba8 (review planned), P44=a7100e80fbc2e881e, P45=a9ba831e40116fe67, P47=ade8cfa7d9af3b85e, P49=ac2731e9da36a5616, P50=a3e02e61f34f6c24b, P53=a814444954c4f01d5, P57=a5e20d963448bd416.
  - Ruling (P41): no cross-stack exports — TrainingKbApp uses `Table.from_table_name`/`Bucket.from_bucket_name` with plain names and deploys `--exclusively`; P57 deploys TrainingKbData `--exclusively` — cost if wrong: two stacks drift on names (bucket name is a fixed deployed value).
  - Ruling (P57): real-bucket demo page allowed (slug `demo-*`, project_id demo) for website-endpoint evidence; objects listed in report — cost if wrong: a labelled demo tutorial remains in the bucket.
- Task P53: complete DONE (commits c798f45..29fb698; 7 own tests; 945 passed/23 skipped/11 xfailed excluding 5 in-progress files). Public: analytics/ratings.py `average_rating`, `cross_version_average`, `negative_feedback_ids`, `format_average`, `NO_RATING_DISPLAY`. Carry-forward: D-44 — P44's `_average` (if any) should import `average_rating` (final fix wave item if P44 lands its own copy).
- Task P44: complete DONE_WITH_CONCERNS (commits 97c3b14..ee631a3; 26 own tests; D-44 honoured via import of analytics.ratings.average_rating). Public: `ReviewMode`, `WeakTarget`, `is_weak(...)`, `select_weak_targets(*, repository, mode, now, thresholds=None)`. Carry-forward: P43 should switch `select_weak_targets` to `approved_categories(repository)` (one-line, commented) — carry into P43 dispatch. In-progress reds observed in others' files (P50 safety_net, P41 ticket_flow, P57 site.py `_diff_block` breaking test_batch_publish_cutpoints) — controller re-runs gate after W1.
- Task P45: complete DONE (commits 6c10294..2aef7a6; 10 own tests). Public: `DIAGNOSE_NODE`, `DiagnosisResult(version_id, step_indexes, reasons, feedback_ids)` (numbers), `diagnose_weak(target, *, repo, writer, operation_id)` (keyword `repo=`), `prompt_diagnose_weak(...)`. Carry-forward to P46: `step_indexes == ()` is legal NO_STEP → no version allocation; duplicate number with different reason → ContentError.
- Task P47: complete DONE (commits db608a1..b422dc8; 22 own tests). Public: `MIN_CANDIDATE_FEEDBACK`, `PROPOSE_NODE`, `CandidateGroup`, `candidate_groups(feedback, approved)`, `candidate_rule_id(group)`, `propose_candidate(group, *, writer, repo, operation_id, rule_id)`, `prompt_propose_rule(...)`. schemas.py untouched (already complete). Note: stray F401 in tests/unit/test_data_stack.py (P57 in progress).
- Task P49: complete DONE_WITH_CONCERNS (commits abe4317..9d56cc1; 33 own tests). Public: `FEATURE_MATCH_THRESHOLD` (alias of Thresholds().cosine_match), `LOCATE_QUERY_NODE`, `LOCATE_CANDIDATE_NODE`, `normalize_feature_name`, `locate_feature(release, *, repository, writer, operation_id) -> Feature | None`, `update_feature_aliases(feature, *, old_name, new_name, repository) -> Feature`. Notes: release.py import block merged/sorted (P50 notified); `_semantic_match` embeds each candidate (no cache) — P52 cost note; real FEATURE#Prepare field comparison handed to P52 §6.
- Task P50: complete DONE (commits 3b19c50..77acf35; full suite 1135 passed/23 skipped/11 xfailed at that moment, ruff/mypy green). Public: `SAFETY_NET_CANDIDATES`, `StepHit(slug, version_id, number)`, `find_release_hits(feature_id, *, repository)`, `needs_safety_net(release, feature, hits)`, `safety_net(release, *, repository, writer, operation_id)`, `prompt_safety_net_confirm(...)`; union done by caller.
  - Ruling: `find_release_hits` may return hits on retired tutorials (P27 `_is_current_published` ignores status; wrapper stays faithful per D-38). UPDATE must never create a version for a retired tutorial → P51 filters retired tutorials at the UPDATE step (skip + recorded reason, with a test); carry into P51 dispatch — cost if wrong: one extra Tutorial read per hit.
- Task P57: complete DONE_WITH_CONCERNS (commits ee41dca..73179b3; full suite 1135 passed/29 skipped/11 xfailed, no ignores). Real AWS: TrainingKbData redeployed; website endpoint http://training-kb-content-example.s3-website-us-east-1.amazonaws.com (site/index.html 200; private prefixes 403); bucket policy = single Allow GetObject on `/site/*`; enforce_ssl Deny removed (website endpoint is HTTP-only); demo objects `site/tutorials/demo-site-check/*`, `site/assets/*`, private `tutorials/demo-site-check/*`, staging `operations/op-demo-site-57/site/*` left and listed in report. `publishing._diff_copy_key` → public `site_diff_key`.
  - Ruling: 00A §6.7 supersedes rule reads "an OLDER published version exists but supersedes is missing → PermanentError" (version-number gaps are legal) — fix at doc sync — cost if wrong: none.
  - Deferred to P60: delete P12 spike leftover `site/tutorials/spike-a/v2.html` (5 B, now publicly readable) after check_public records it.
  - Carry into P42 dispatch: complete the `import_feedback` half of tests/integration/test_site_widget_roundtrip.py (TODO at file head).
- Task P41: implemented DONE (commits 040ffbb, d7670dc, 98c3a83, 07a4c6a; suite 1135/29/11; 6 real-AWS tests passed). Deployed: TrainingKbApp (state machine arn:aws:states:us-east-1:123456789012:stateMachine:training-kb-ticket-analysis; Function URL https://examplewebhookurlid0000000000000.lambda-url.us-east-1.on.aws/; log group /aws/vendedlogs/states/training-kb-ticket-analysis; Lambda training-kb-pipeline-task x86_64/512MB/90s; layer `training-kb-deps` via `uv run python -m infra.scripts.build_lambda_layer` — must run before synth; stack exposes `self.deps_layer`). Evidence: injected TransientError ×3 with 1s/2s intervals → ErrorEquals hit; O5 BLOCKED evidence recorded (NameGap PermanentError; EnsureEmbedding ValidationException via webhook path); NotRecurring SUCCEEDED; idempotent resend → same operation/execution.
  - Ruling: history field for direct-ARN Lambda tasks is `lambdaFunctionFailedEventDetails` (no TaskFailed events) → 00A §3.7/§8 wording fixed at doc sync; carry into P48/P52/P59 dispatches — cost if wrong: none.
  - Ruling: infra/app.py skips the app stack when prerequisites (bucket name / layer) are missing instead of failing synth — keeps data-stack-only deploys working — cost if wrong: a silent skip during a full deploy; report notes it.
  - Note: synthetic seeds t_881/882/883/t_901–903 + one active PROC left in the production table (P56/P60 aware).
  - Review dispatched (opus) with reviews/P41-review-package.md. W2 dispatched in parallel (fixes, if any, go back to the P41 agent; P42/P54 edit the stack file concurrently under R3).
- Controller gate after W1 (HEAD 90df9d9): see below. P41 reviewer=a6fe23cff46a0e87d. W2 dispatched at BASE 90df9d9 (opus ×4): P42=aa58dd5c3757a91cc (review planned), P46=a898fbf865e57e982, P51=aa23d6cbd05bfebe9, P54=ac333b55f81b9a766.
- Gate after W1 verified by controller: `uv run pytest tests -q -W error` → 1135 passed / 29 skipped / 11 xfailed; ruff clean; mypy clean (43 files).
- P46 paused after reading; resumed with rulings. Ruling: `StepRewrite` gets tutorial-writing max_tokens 2048 (00A §3.7 lists P46/P51) → P46 adds it to `WRITING_MAX_TOKENS` and updates the P18 assertion in tests/unit/test_writing_validation.py (R3.6 exception, one line; 00A sync at closing) — cost if wrong: one extra 2048-token budget on a judgement-sized call. Ruling: P46 unit fixtures use moto + real Repository subclass (pattern of test_publisher_single.py) — cost if wrong: slower unit tests.
- Task P41 review: Needs fixes — Important: (1) webhook role over-grant (site/* write, table-wide Scan/Put/Update); (2) layer guard satisfied by empty dir created by tests; (3) blocked publish returns normally → SUCCEEDED at `Published`; (4) aws integration test is a snapshot (no seed/start/cleanup; relative ASL path; bare load_settings bucket fallback). Fix round 1 sent to P41 agent (resume) with minors bundled: TKB_ENV in base_env (default prod), webhook bedrock:InvokeModel scoped, naming["gap"] KeyError→PermanentError, ten-field assertion on CREATE path, no real boto3 in unit test, log retention 3 months. Deferred minors: non-dict state coercion; ensure_embedding/_stored_ticket coupling. FIX_BASE for re-review = 07a4c6a (P41's last commit).
- Task P54: complete DONE (commits 23d8c68..90830cb; 26 own tests; suite 1313/31/11 at that moment). Deployed training-kb-analytics (invoke evidence in report). O4: implemented per design §12.1 recommendation, marked 未核定 (maintainer sign-off pending). Public: analytics.reopen (`reopen_window`, `ReopenStats`, `reopen_stats`), analytics.rules_metrics (`RULE_STATUS_KEYS`, `rule_counts`, `applied_count`, `bedrock_call_count`), analytics.version (`NO_REOPEN`, `VersionMetrics`, `version_metrics`, `metrics_action`), handlers.analytics (`handler`, `_wiring`/`_reset_wiring`), stack `self.analytics_function`.
  - Ruling: analytics Lambda IAM includes PutItem/UpdateItem + operations/* because P55's validate_rules writes RULE.status through the same function (D-58: P55 does not touch CDK) — cost if wrong: two lines to tighten.
  - Carry into P43 dispatch: `handlers/analytics.py::_approved_categories` resolves P43's `approved_categories` lazily (falls back to frozenset()); P43 must add a test proving the analytics handler picks up the CONFIG table once P43 lands.
- Task P41: fix round 1/5 (10 addressed, 0 open; commit 6a17148; suite 1317/31/11; aws test self-contained, new execution op-ticket-t_p41sm1789448254 SUCCEEDED). `_grant_data(prefixes, actions, with_index)` now parameterised (defaults = widest) — P42 told to pass the minimal set. Scoped re-review dispatched with reviews/P41-fix1-package.md.
- INCIDENT (W2, 21:47–21:49): P54 `commit --amend` (d6f54d9→14b78b0) then `reset --to d6f54d9` dropped its own amended commit 14b78b0 and P46's 4d8d3c4 from the branch; P51 committed on d6f54d9 afterwards. P46 re-committed its lost work (1f4af65 also restored P51's `prompt_release_rewrite`); P51 noted the incident (c6f799b). Ruling: amend/reset/rebase now explicitly forbidden in COMMON.md R3.4; running agents (P42, P51) told — cost if wrong: none. Controller verifies lost-content restoration (see next line).
- Task P46: complete DONE (commits 0a4f99b, a4cdda4, 889bfe8, 1f4af65, 2d888aa; suite 1317/31/11; real-AWS refine retry test passed on production table with demo prefix p46-refine-20260914d1). Public: `REFINE_NODE`, `RefinePlan(...)`, `evidence_fingerprint`, `refine_operation_id`, `refine_reason`, `evidence_of(diagnosis, *, repo)`, `prepare_refine(diagnosis, *, repo, writer, operations, operation_id) -> RefinePlan | None`, `prompt_refine_steps(base, diagnosis, category, rules_block)`. `WRITING_MAX_TOKENS["StepRewrite"]=2048` landed with P18 assertion update (00A sync pending).
- Incident verification: lost 14b78b0 was P54's amend that merely stripped P42's in-progress ingress.py lines from d6f54d9 (losing it is harmless; d6f54d9 carries P42's partial edits per R3.3). P46's 4d8d3c4 content re-landed via 1f4af65/a4cdda4 (own tests green; residual line differences are later refactors). No work lost. P42/P51 notified of the amend/reset ban.
- Task P51: complete DONE_WITH_CONCERNS (commits b40b0fd, dd05456, e6b6a96, 073da4f, c6f799b, 9f5f35d; suite 1318/31/11). Public: `REWRITE_NODE`, `assert_unchanged(base, draft, changed)`, `prepare_update(release, hits, *, repository, writer, operations, operation_id) -> tuple[VersionPlan, ...]`, `prompt_release_rewrite(release, base, targets, rules_block)`. Retired tutorials skipped (ruling honoured). aws retry test written, not executed (P52/P59 exercise the real path). Correction: amend 5c1fb97 (rewrote P42's e5ef9c6 with P51's 3 report lines) was P51's, before the ban; content intact. Carry into P52 dispatch: pass an O2-accepted parent operation_id; union of hits by caller; one PublishRequest for all version_ids (F49).
- W3 partial dispatch at HEAD ce67113 (deps satisfied; P43 waits for P42): P48 (review planned), P52 (review planned), P55 — agent IDs recorded on next line after launch. Ruling: P42's import Lambda grants states:StartExecution on BOTH machine ARNs by name (format_arn), so P52 never touches the import Lambda — cost if wrong: none. Ruling: P48's Lambda-name assertion is a superset check excluding training-kb-import (P42 lands concurrently) — final review may tighten — cost if wrong: none.
- W3 partial agents (opus): P48=a04329098ef097ffb, P52=a365823580476e626, P55=ad215d2d4af3d7080. Still running from W2: P42=aa58dd5c3757a91cc; P41 re-review=aa64b110b171967f9.
- Task P41 re-review: all 4 Important + 6 minors ADDRESSED. New Important (fix-diff side effect): tests/unit/infra/test_import_lambda.py (P42) and test_analytics_stack.py (P54) mkdir an empty layer dir → pass only when a real layer exists locally → routed to P42 (still active; also fixes P54's file as R3.6 exception) via shared `fake_layer` fixture in tests/unit/infra/conftest.py; P48/P52 told to use it. Task P41: complete (review clean after round 1; commits 040ffbb..07a4c6a + 6a17148; stack half of the fix landed inside P54's c455d26 per R3.3).
  - Deferred (final review): `_grant_data` defaults still widest (make keywords required?); MARKERS don't detect wrong-platform pydantic_core; no unit test for `_gap_of` PermanentError; integration execution name epoch-second collision; docstring drift (S3 5xx wrapped into PublishError; skip wording); P41 report §8 commits table lacks 6a17148/c455d26 note.
- Task P42: implemented DONE (commits ad3e5e5, a35393d, d807c62, da95e4c, 5c1fb97, ce67113, 2fc047b, 0b2dc2d, 3a17b16; suite 1364/31/11; training-kb-import deployed + 3 real invokes with evidence; import role: S3 operations/* only, no site/, StartExecution on two named machine ARNs). Public: `FEEDBACK_FIELDS`, `VIEW_FIELDS`, `FEEDBACK_ID_PREFIX`, `ImportResult`, `validate_feedback(payload, *, now)`, `validate_view(payload)`, `import_feedback(payload, *, repository, operations, now)`, `import_view(...)`, handlers.import_ `handler`, `IMPORT_KINDS`, `IMPORT_DEADLINE_SECONDS`. Shared `fake_layer` fixture landed in tests/unit/infra/conftest.py (P41 re-review item closed). Incident attribution corrected: the W2 amend+reset was P42's (Task 1), not P54's — content restored; ban in force. Review dispatched with reviews/P42-review-package.md. P43 dispatched (W3).
- P42 reviewer=a37b4eff964b5577c; P43=a1ff9fb683e667a22 (W3, opus). Running: P48, P52, P55, P43, P42-review.
- Task P42 review: Needs fixes — Critical: META written but REFERS_TO edge lost → dedupe short-circuit never repairs (feedback invisible to by_target consumers, ledger stuck accepted). Important: batch writes half before shape abort; per-item deadline never fires. Minors: DescribeExecution for import role; stale test docstring; `f_` prefix = 本計畫選擇; info log line. Fix round 1 sent to P42 agent (resume). Ruling: shape errors → whole batch PermanentError before any write; ticket/release IngressError → per-item rejected — cost if wrong: batch semantics differ from doc wording (doc updated by P42). FIX_BASE = 3a17b16.
- Task P55: complete DONE_WITH_CONCERNS (commits 6975e9d, 07fa5e8, 4fcb744, 9750928, 1379aa1; suite 1368 passed excluding P48/P52 in-progress files; only red = P48's placeholder test in test_ticket_flow.py that P48 must flip). Public: analytics.validation (`Verdict`, `SeedBatch`, `RuleEvaluation`, `evaluate_batch(batch, *, approved, repository)`, `validated_conflict`, `next_status(current, evaluations, conflict)`, `curation_groups`), status_writer (`LEGAL_TRANSITIONS`, `record_evaluation`, `apply_rule_status(rule_id, status, *, repository, now)`), handlers.analytics `validate_rules_action` + handler branch. Accepted: one-line change to P54's test_reopen_metrics.py ("validate_rules" no longer an unknown action → "curation"). Cloud invoke of validate_rules left to P59/P60 (Lambda asset predates the commit). 00B VAL Rule 3 test-name fix at doc sync (already ruled).
- Task P52: implemented DONE_WITH_CONCERNS (commits 44277f8, 6cd4261, 4b49ee4, 9d4b97d, b8cc71f; suite 1454/45/11 no ignores; 8 real-AWS tests passed). Deployed training-kb-release-update (STANDARD). Cloud evidence: RETIRE SUCCEEDED + idempotent resend + injected TransientError ×3 → PipelineFailed; O5 BLOCKED at PrepareUpdate/SafetyNet/LocateFeature(semantic). D-83 evidence: retired index page live on website endpoint with successor link, v1.html untouched. Demo objects listed in report. Accepted: three count assertions in P41/P54 infra tests relaxed to inclusion (documented). Rulings: `retire.json.successor` records the requested value (documented); `removed` with zero hits still RETIRE (retire is feature-level) — cost if wrong: none. KEEP path in cloud needs Titan (safety net) → BLOCKED until O5. Review dispatched with reviews/P52-review-package.md.
- P52 reviewer=a5c1aab165616a789. Running: P42 fix round, P43, P48, P52-review. W4 gating: P56 needs P43; P59 needs P48.
- Task P48: implemented DONE_WITH_CONCERNS (commits 19ab65d, ea9cde7, e418f8b, bcf4fc6, 222bc1f; stack section swept into P43's fe309bb; suite 1454/45/11; 6 real-AWS tests passed). Deployed training-kb-feedback-review + schedule training-kb-feedback-review-daily (cron(30 0 * * ? *) UTC, {"mode":"formal"}). Cloud: SUCCEEDED zero-target run (0 Bedrock); O5 BLOCKED evidence at EvaluateTargets. O3 stays FAIL; publish cut points never reached in cloud → P59. Carry-forward: P43 must switch `task_evaluate_targets` + `select_weak_targets` to `approved_categories`; 00A §14.2 note: same-day resend of a succeeded review yields no new version (F23). Review dispatched with reviews/P48-review-package.md. P59 dispatched (W4).
- Task P42: fix round 1/5 (8 addressed, 0 open; hunks inside fe309bb [P43's sweep] + 26e02b1, 5a390c2, 434cbeb; suite 1454/45/11; redeployed; real invoke ×2 with resend evidence). Deferred (final fix wave): Lambda `LoggingConfig` so INFO lines reach CloudWatch (affects all four functions; P41-owned `_function()`). Scoped re-review dispatched with reviews/P42-fix1-package.md.
- NOTE: P43 (running) committed fe309bb sweeping other phases' uncommitted hunks (P42 fix, P48 stack section) — content intact; P43 reminded to add only own paths.
- Dispatched: P42 re-review=aac2046b6fb5d0dd6; P48 reviewer=a452815ed6ebd53cf; P59 (W4, review planned)=ab1c7479a69373ab2. Running also: P43=a1ff9fb683e667a22, P52 reviewer=a5c1aab165616a789. P56 waits for P43.
  - Ruling (P59): real-AWS recovery drills run the library code locally (`TKB_ENV=demo TKB_FAULT=<point>` in the local process) against the real table/bucket/state machines — the five cut points are in-process, so no Lambda redeploy is needed; in-Lambda retry semantics already proven by P41/P52 — cost if wrong: drills exercise the same code but not the Lambda runtime wrapper.
- Task P52 review: Needs fixes — Important: (1) `publish_request_ref` dangling (object never written on release path); (2) no positive-path task tests for prepare_update/publish_batch/update_aliases (alias collision row is doc-mandated). Minors: retire.json check-then-write → catch ObjectAlreadyExists; tighten relaxed P41 log-group test; module-level skip without bucket; docstring. Fix round 1 sent to P52 agent (resume). FIX_BASE = b8cc71f.
  - Deferred to final fix wave (cross-cutting, all three pipelines): cloud per-task handlers bypass `run_sequence`, so failures never reach `OperationCoordinator.fail` → Ruling: fix once in `pipeline_task_handler` (record fail + re-raise, mirroring run_sequence) after checking `operations.py` transition rules (retry then success must still be able to `complete`) — cost if wrong: ledger shows failed for an execution that later succeeded on retry.
- Task P42 re-review round 1: 7/8 addressed; open = Important 2 half (source-field/payload shape checks still inside write loop on ticket/release branch). Fix round 2 sent (hoist checks + test; doc 整批/整筆 wording; sts comment). Deferred: `_assert_open`/`get_version` run before `_dedupe` (retired-after-strand corner); `_record` only logs on success; P41 `_grant_execution_lookup` sts statement is dead weight (AWS: no permission needed); import ticket/release branch never exercised in cloud (O6). P43 to fix stale doc item 3 in Phase 42 doc.
- Task P48 review: Approved with findings — Important: (1) approved_categories divergence (already fixed by P43's 33b2582; record correction only); (2) `prepare` re-run in all three publish Tasks (Inspect not read-only, 3× cost); (3) `review-result.json` overwritten on same-day rerun (audit loss). Minors: commit not retry-safe; `_review_mode` silent default; dead `assert_batch_publishable`; triple copy of target predicate; sub-operations never completed; checkbox honesty. Fix round 1 sent to P48 agent (resume). FIX_BASE = 222bc1f. Reviewer confirmed live: scheduler role single-statement, snapshot byte-identical, zero residue.
- Task P43: complete DONE (commits 08371f2, 6111141, fe309bb [sweep], 33b2582, 7fa230c; suite 1455/45/11). Public: `PENDING_CATEGORY`, `FEEDBACK_CATEGORIES_PK`, `CLASSIFY_NODE`, `approved_categories(repository)`, `classify_feedback_category(feedback, *, approved, writer, operation_id)`, `import_feedback(..., writer=None)`, `prompt_classify_comment(comment, approved)`. Import role now has bedrock:InvokeModel (embedding ARN only until O5 clears — two places to extend: P41 + P43). Sweep commit was explicit paths on shared files carrying others' unstaged lines (R3.3 accepted).
- W4: P56 dispatched (opus) — agent id on next line. Ruling: P56 computes all eight seed targets now (P43/P54/P55 landed; no xfail placeholders) — cost if wrong: none. Ruling: `demo/` importability via pytest `pythonpath=["."]`, mypy files += demo, ruff paths += demo — cost if wrong: config churn.
- P56=a89a10c8e2a8bf9a1. Running: P59, P56, P52 fix, P42 fix 2, P48 fix. W3 core (P43, P55) complete; P48/P52 in fix loops.
- Task P42: fix round 2/5 (1 addressed, 0 open; commits 439cd94, 420a761, d5cc596; suite 1466/45/11). NOT redeployed (code change to handlers/import_.py) → closing step must redeploy TrainingKbApp once so P60 evidence matches HEAD. Scoped re-review 2 dispatched.
- Task P52: fix round 1/5 (6 addressed, 0 open, 1 deferred by ruling; commits 5de25c2, 46c5a73; suite 1466/45/11; redeployed TrainingKbApp so the Lambda asset matches HEAD as of that commit; 8 aws tests green). Scoped re-review dispatched.
- Task P42 re-review round 2: ADDRESSED, no new breakage. Task P42: complete (review clean after round 2; commits ad3e5e5..3a17b16 + fe309bb hunks + 26e02b1, 5a390c2, 434cbeb, 439cd94, 420a761, d5cc596). Deferred minors (final fix wave / doc): stale rationale in tests/unit/infra/test_import_lambda.py:197-202; report §1.2 names `_item`; handler docstring overstates per-item catching (only IngressError caught; PermanentError/TransientError from Rote escape mid-loop — consider wider except or wording).
- Task P52 re-review: all 6 ADDRESSED. Task P52: complete (review clean after round 1; commits 44277f8..b8cc71f + 5de25c2, 46c5a73). Final fix wave item: release `publish-request.json` must include `"prepared_at": to_iso(prepared.prepared_at)` (P48's `_restored` reads it; in-code claim of identical shape is false) — one line at release.py ~939-941 + key-order note.
- Task P48: fix round 1/5 (8 addressed, 0 open; commit cd10f4e; suite 1482/45/11 excluding P59/P56 in-progress test files; 6 aws tests green). Not redeployed (P59's publishing.py in flux) → closing redeploy covers it. Scoped re-review dispatched.
- Task P48 re-review: all 9 ADDRESSED. Task P48: complete (review clean after round 1; commits 19ab65d..222bc1f + fe309bb hunks + cd10f4e). Final fix wave items: (A) doc/report sweep — REP §1.1/§7.2 and Phase doc §7 Task 2 sketch + test docstring at test_feedback_review_flow.py:417 still describe `_prepared`/re-prepare/non-merging result; (B) harden `_publish_split` skip branch: require the batch to be published under THIS operation and all `public_site_keys(prepared.version_ids)` objects to exist before treating as success (redrive/manual re-invoke window); (C) `_restored`/`_review_result` bare json.loads/KeyError → PermanentError for legacy/corrupt records.
- Task P59: implemented DONE_WITH_CONCERNS (commits 238bd38, 22047b5, d51b735, 8b0ff84, bdf4338, c79904f; suite 1526/49/11; check_asl passes on three ASL files). Real-AWS drills in docs/plan/report/recovery-20260915-0644.md: s3_after_md PASS, ddb_after_version PASS, publish_before_transact PASS, a2 FAIL(O3)+resume effective, a3 multi FAIL(O3 partial A 200/B 404, recorded, not relaxed), start_execution PASS continuation + O5 BLOCKED. `InjectedFault` now an alias of TransientError (00A §4.1/1230 wording at doc sync). Incident: first aws run overwrote P57's shared site/index.html — restored byte-identical + fixture guard. P60 deploy checklist: TKB_ENV=prod, no TKB_FAULT/TKB_FAULT_TASK. Review dispatched with reviews/P59-review-package.md.
- P59 reviewer=a995a36047298e038. Running: P56, P59-review. Next: P58 (W5) after P56; P60 (W6) after P58; then final review → fix wave → closing redeploy → doc sync → REP.
- 01:1x PDT 2026-09-15: opus session limit (429) killed P56 (mid Task 2 docs) and the P59 reviewer; limit reset 12:50am. P56 resumed via SendMessage (its commits ebcb332, b5ce04a, 971942a already landed); P59 reviewer re-dispatched = aae7d7b98b56f15fe.
- Task P56: complete DONE_WITH_CONCERNS (commits ebcb332, b5ce04a, 971942a, 1e6ca1e, 34a3f56; suite 1526/49/11; ruff incl. demo + mypy 54 files green). O7: schema PASS, recompute PASS (eight targets), approvals MISSING (R007-B1, R012-B1, R012-B2) — maintainer sign-off pending; O5 BLOCKED evidence in demo/seed/clustering_report.json. Ruling accepted: seed NOT applied to the real table (would alter active rules/public site; no cleanup path). Interface for P58: `SeedBundle.contents`, `batches: tuple[SeedBatch,...]`, unapproved → `approved_by/approved_at = None`. COMMON/IMPLEMENTER lint line now includes demo. P58 (W5) dispatched.
- P58=a23a8abdb37550cf1 (W5). Running: P58, P59-review. Plan: P60 after P58 (checks.py + first evidence index) → final whole-branch review → fix wave → closing redeploy of TrainingKbApp → re-run P60 `run_all` to regenerate evidence → doc sync (00/00A/00B) → REP → TODO.
- Task P59 review: Needs fixes — Important: (1) check_asl CLI ignores argv (silent exit 0 for unrequested files); (2) §11 multi-article "五個切點" checkbox green on evidence covering only a3 → run remaining batch cut points on real AWS (website A/B HTTP evidence) or narrow the text honestly. Minors bundled: prod guard normalisation; stale doc snippets (InjectedFault/ValueError); resume_publish docstring table; moto assertions for cut points 1/2 + resume idempotency; a3 label docstring; aws seed must not rewrite shared site/index.html; run-id suffix for aws ids. Deferred: private-method index rebuild (`rebuild_indexes`), comprehension side effects, lease test narrower than doc. Fix round 1 sent to P59 agent (resume). FIX_BASE = c79904f.
- Task P59: fix round 1/5 (2 Important + 7 minors addressed, 0 open; commits e167133, 73e9061, 67b9508, 5193d71, 8940541; suite 1559/49/11 excluding P58 in-progress file). Multi-article cut points 1/2/3/4a/4b run on real AWS (A/B both 404 during injection, 200/200 after resume → PASS); cut point 5 = 200/404 partial → FAIL(O3) kept. New regression: batch interrupted before transaction has no resume input (CoordinationError; upstream replays with same version_ids). Scoped re-review dispatched.
- Task P59 re-review: all ADDRESSED; no functional regression. Task P59: complete (review clean after round 1; commits 238bd38..c79904f + e167133, 73e9061, 67b9508, 5193d71, 8940541). Final fix wave items: (1) guard `_cleanup` in tests/integration/test_recovery.py against empty `World.prefix` (would wipe the real table/bucket if ever called on a default World); (2) doc nits: P59 §7 Step 4A item 2 still says a3-only; docstring anchor at test_recovery.py:522 → 附錄 A.4; appendix A.1 wording (c1-b vs A@v3); resume_publish docstring cut-point-1 mechanism (CoordinationError row, not verify_version_complete); P25 §11 deferred checkbox may now be ticked.
- P59 safety follow-up: 8ae095a (`_cleanup` refuses empty prefix + test; docstring anchor). P59 fully closed. Running: P58 only.
- Task P58: complete DONE_WITH_CONCERNS (commits 74fa3c1, fb40ef0, 9b62542, e8caacc, e3c5e6d, a59f209; suite 1583/49/11; ruff incl. demo + mypy 58 files green). Public: cli (`SUBCOMMANDS`, `build_parser`, `main`, six subcommands), preview (`PREVIEW_PREFIX`, `PreviewResult`, `run_rule_toggle_preview`), view_model (`DASHBOARD_BLOCKS`, `SYNTHETIC_NOTICE`, `Banner`, `render_banner`, `CallBreakdown`, `call_breakdown`, `dashboard_view`, `load_dashboard`), dashboard (AST-guarded). Preview isolation proven on moto (no formal item/site change; model_calls 2). Concerns: D-68 wording vs seed (share-summary already active → KEEP); seed lacks operations/rules/validated_at.json (run validate_rules before rehearsal); manual-import sources O6-pending. P60 (W6) dispatched.
- P60=abd1e8ab61f1407e9 (W6, running). Final review split into 3 areas: A (P42–P48) and B (P49–P55) dispatched now at 2714dcd in parallel with P60; C (infra/site/demo/faults/checks: P41, P56–P60) after P60. Packages reviews/final-A.md, final-B.md.
- Final reviewers: A=a347b26d63f2f6695, B=af51377fbab84da14 (opus). Running: P60, final-A, final-B.
- Final review A: Mergeable after fix wave — 1 Critical (commit skip branch claims published without site objects), 6 Important (orphaned unpublished versions on rerun; diagnose before F23 short-circuit + unpersisted diagnosis; cloud handlers never `operations.fail`; feedback ids unescaped in prompts; import batch contract mismatch; release publish-request lacks prepared_at) + promoted ledger items (P48 C json/KeyError → PermanentError; P48 A doc sweep). Findings file: reviews/final-A-findings.md. Ruling: import per-item catch widened to PermanentError/CoordinationError (TransientError propagates) — cost if wrong: a batch row marked rejected for a transient-looking permanent error.
- Final review B: Mergeable after fix wave — 4 must-fix (release publish-request prepared_at + false claims; analytics Lambda PutItem over-grant; cloud handlers never operations.fail (operations._change has no transition guard → safe); validate_rules per-invocation grouping can never retire). Findings file: reviews/final-B-findings.md. Note: ruff currently red on P60's untracked test file (in flight).
- Task P60: complete DONE_WITH_CONCERNS (commits b7fbff9, 363ff41, 7d329fa, 5a394a2; suite 1630/49/11; deployed TrainingKbApp at 2714dcd). run_all: secrets pass; iam data pass; iam app FAIL (sts:GetCallerIdentity Resource:*); public pass (site/* only); output-safety pass; endpoint pass; snyk not_run (unauthenticated, exit 3); pip-audit FAIL (pytest PYSEC-2026-1845, dev dep); rehearse: model call not_run (O5). Acceptance rows=168 pass=132 fail=4 not_run=32 → 最終驗收未完成 (honest: O3 FAIL, O5/O6/O7). 147 Rules coverage 0 gap. Spike leftover deleted (site/ 16→15). Outputs: infra/scripts/checks.py, evidence.json, acceptance-20260915T092932Z.md, snyk-20260915T092728Z.md, security-20260915T094307Z.md, screenshots.
  - Ruling: remove the app-stack sts:GetCallerIdentity (Resource *) grant (no permission required per AWS) instead of relaxing check_iam — fix wave — cost if wrong: none (documented by AWS).
  - Ruling: upgrade pytest within >=8,<9 to clear PYSEC-2026-1845 if a fixed release exists (fix wave: uv lock --upgrade-package pytest, rerun suite; controller's accidental run produced no lock change); else record — cost if wrong: dev-only dependency churn.
  - Ruling: MET#10/11/12 rows → not_run under O7 (consistent with 未核定 seeds) — fix wave, three-line override — cost if wrong: three rows under-reported.
- Final review C dispatched at 5a394a2 with reviews/final-C.md.
- Final reviewer C=a469a9acfb4133c31. Fix-wave brief drafted at reviews/fixwave-brief.md (rulings 1–15 + closing redeploy/evidence steps); final-C-findings.md to be written when C reports; then ONE fix-wave agent (opus).
- Final review C: Mergeable after fix wave — 7 must-fix (Critical: app-stack sts Resource:* grant; Important: demo/ un-gated production writes ×2; maybe_fail_task prod-guard divergence; check_secrets excludes docs/; MET#10/11/12 green under O7; pytest advisory; resume_publish tautological guard). Confirms A/B items. Findings file: reviews/final-C-findings.md. Closed by verification: spike leftover 404; _cleanup guard.
- FIX WAVE dispatched (opus, single agent) with reviews/fixwave-brief.md + final-A/B/C-findings.md; FIX_BASE = fc1fa25. Agent id on next line.
- Fix-wave agent = a8ac17e9d139fd018 (opus). Doc-sync + REP-draft agent dispatched in parallel (edits only 00/00A/00B + writes the batch REP; no overlap with the fix agent's files).
- Doc-sync/REP-draft agent = a83bd421ef44bb520 (opus). Running: fix wave, doc-sync. After both: controller gate + scoped re-review of the fix wave → fill REP §6 → tick TODO → archive ledger/COMMON into docs/plan/report → final message with all rulings.
- Doc sync complete (65b5f9b, 0374c86): 00A D-84..D-107 added; §3.1/3.2/3.5/3.7/4.1/4.2/6.7–6.11/§7 updated; 00B VAL (§2.13, not §2.12) Rule 3 test name fixed; 00-總覽 gate table + batch status; REP draft `docs/plan/report/2026-09-14-Phase41-60實作-REP.md` with §6 placeholders. Contradictions resolved in favour of the later rulings (25 over 12; 32 over 16; final-B over 39) — recorded in REP §2.2. After the fix wave: update REP §1/§6/§7 and 00-總覽 statistics (132/4/32 → expected 129/4/35), turn REP §9 FixWave row into a link.
- FIX WAVE complete DONE_WITH_CONCERNS (commits 79b9306, 8319bda, 0a6e9ab, 5f8d214, 163708c, cdbaf97, 58521bd, 72055b3, 4b8c3e3, cfe1371, 080cc2e, a878904; agent-reported suite 1703/49/11, ruff/mypy green, check_asl exit 0). Deployed TrainingKbApp at 080cc2e (webhook role verified: no sts grant). validate_rules cloud invoke ×3 recorded (empty → 200; malformed → PermanentError; seed batches → "找不到規則：R-007" because O7 unsigned → no RULE items). checks: security 7/7 pass; scan 2 not_run (snyk unauth; pytest advisory has no in-range fix, uv.lock unchanged); rehearse 3 pass/1 not_run (O5); acceptance 168 rows pass=129 fail=4 not_run=35. Three aws state-machine tests re-run: 20 passed.
  - Ruling: `_publish_split` "published under this operation" = pending-promote.json (multi) OR ledger version_id (single), since 00A §6.7 never writes pending-promote for single-version batches — cost if wrong: a single-version redrive after a2 could be treated as published; mitigated by the site-object existence check.
  - Ruling: a stored refine reply whose step set no longer matches the (persisted) diagnosis is discarded and the model re-called (instead of failing forever) — cost if wrong: one extra model call on a rare path.
  - Deferred (documented in FixWave REP §8): check_iam partial wildcards/ManagedPolicyArns; check_public list principal + Block Public Access; `_row_status` wording; non-prod synth guard.
- Scoped re-review of the fix wave dispatched with reviews/fixwave-package.md. Controller gate running in background.
- Fix-wave re-review: ALL ADDRESSED, no new breakage. Controller gate at a878904: 1703 passed / 49 skipped (48 aws + 1 O5) / 11 xfailed; ruff clean; mypy 59 files; check_asl exit 0. REP §6 filled (2b7ea7a).
  - Parked — Ruling: failures raised before `deps_for` on a cold container (injected TKB_FAULT_TASK, unknown task) are not ledger-recorded; real failures occur after deps exist; pre-fix recorded nothing — accepted as residual, documented in FixWave REP §9 — cost if wrong: a cold-start injected fault leaves no ledger row.
  - Parked — Ruling: `TKB_FAULT_TASK` validates shape + pipeline name only; a typo'd task name silently injects nothing — accepted (drill-only switch; deploy checklist requires it unset) — cost if wrong: a mis-typed drill that appears to pass.
  - Parked (documented in FixWave REP §8.2): 17 minors incl. check_iam partial wildcards/ManagedPolicyArns, check_public list principal + Block Public Access, `_row_status` wording, non-prod synth guard.
- CLOSING: TODO fully ticked; ledger + COMMON + briefs + final findings archived under docs/plan/report; workspace deleted per skill. Branch backend NOT pushed (167+ commits since 9bebda6).
