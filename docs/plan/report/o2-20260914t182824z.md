# O2 報告 20260914t182824z

Region：us-east-1｜表：training_kb_o2_20260914t182824z｜run id：20260914t182824z

| 案例 | 注入點 | 期望 | 觀察 | 判定 | 證據 |
|---|---|---|---|---|---|
| restart | after_record_version | 同 operation_id 與同 version_id | killed=-9 child_version=o2-restart-96b2843a@v2 resend_status=duplicate version_id=o2-restart-96b2843a@v2 current_version=o2-restart-96b2843a@v2 ops=1 accept_seq=1 | PASS | operations/o2/20260914t182824z.json |
| resend | after_accept | status=duplicate，OPS 只有一筆 | statuses=accepted,duplicate,duplicate,duplicate duplicate=3 accept_seq=1,1,1,1 ops=1 backfilled=1 version_ids=o2-resend-456616ee@v2 seq_counter=4 | PASS | operations/o2/20260914t182824z.json |
| lease_expiry | owner_vanishes | 到期後可接手，順序仍照 accept_seq | held=True before_expiry=False after_expiry=True readable_after_expiry=True stale_owner=worker-a ttl_attr_set=True accept_seq=1,2 order=op-release-a-1b05b586,op-feedback-b-1b05b586 owner_after_release=(empty) | PASS | operations/o2/20260914t182824z.json |
| interleaved | release_and_feedback_same_slug | 兩版串行且無分叉 | accept_seq=1,2 lock_order_reversed=True assigned=op-release-r42-ac1a1d8f>o2-inter-ac1a1d8f@v3,op-feedback-ac1a1d8f>o2-inter-ac1a1d8f@v4 current_version=o2-inter-ac1a1d8f@v4 distinct_versions=2 ops=2 | PASS | operations/o2/20260914t182824z.json |
| closed_execution | already_exists_closed | 讀 ledger 原結果；無結果則明確失敗 | execution_arn=op-release-done-2539ce5d resumed_version=o2-closed-2539ce5d@v2 no_result=CoordinationError empty_version_id=(none) current_version=o2-closed-2539ce5d@v2 ops=2 | PASS | operations/o2/20260914t182824z.json |

整體判定：PASS

## 讀這份報告要記得

- **鎖不等於接受順序。** 誰先搶到 lease 只代表誰先送達 DynamoDB；順序一律讀 `accept_seq`（`interleaved` 案例的搶鎖順序刻意與接受順序相反）。
- **TTL 不是準時解鎖。** 過期項目在刪除前仍讀得到（`lease_expiry` 的 `readable_after_expiry`）；到期一律自己比 `expires_at`，再用 `expected_revision` 搶佔。
- 版號由本 Phase 的替身產生（讀 `current_version` -> 加一 -> `record_version`），真正的 `allocate_version` 屬於 Phase 20；`closed execution` 用最小 stub，不實作 `PipelineStarter`（Phase 32）。
- 重送會燒掉一個接受號碼：`accept_seq` 只保證單調遞增、可比較，允許缺口。

## 隔離測試表生命週期

表名：`training_kb_o2_20260914t182824z`（PK／SK 同正式表、PAY_PER_REQUEST、TTL 屬性 `ttl`）；正式 `training_kb` 表完全不碰，只有 gate 證據寫進正式 bucket 的私有 `operations/o2/` 前綴。

| 步驟 | 時間／輸出 |
|---|---|
| 沿用既有隔離表 | 2026-09-14T18:29:17Z（`--provision` 先建立的同一張） |
| 五個案例跑完 | 2026-09-14T18:29:29Z |
| 刪除 | 2026-09-14T18:29:50Z |
| 刪除後確認 | `table_exists` -> False |
| 證據物件 | s3://training-kb-content-example/operations/o2/20260914t182824z.json |

## 重現指令

```bash
# 1. 建立隔離表（印出三個環境變數）
TKB_AWS_REGION=us-east-1 uv run python infra/scripts/o2_report.py \
    --provision --region us-east-1 --run-id 20260914t182824z
# 2. 五個案例對真實表跑一次
TKB_RUN_AWS_INTEGRATION=1 TKB_AWS_REGION=us-east-1 TKB_TABLE_NAME=training_kb_o2_20260914t182824z \
TKB_O2_RUN_ID=20260914t182824z uv run pytest tests/integration/test_o2_cases.py -q -m aws
# 3. 產報告與證據，跑完自動刪表
TKB_AWS_REGION=us-east-1 TKB_CONTENT_BUCKET=<正式 bucket> \
uv run python infra/scripts/o2_report.py --region us-east-1 --run-id 20260914t182824z
# 4. 人工複核三種 item（需先用 --keep 保留表）
aws dynamodb get-item --region us-east-1 --table-name training_kb_o2_20260914t182824z \
    --key '{"PK":{"S":"OPS#<operation_id>"},"SK":{"S":"META"}}'
```
