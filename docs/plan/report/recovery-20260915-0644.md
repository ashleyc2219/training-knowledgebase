# 失敗復原演練紀錄 — 2026-09-15 06:43–06:45 UTC（Phase 59 Task 3 Step 4A）

> **這份紀錄的結論不得被讀成「發布故障驗收已通過」。** O3 由 Phase 12 判定為 **FAIL**
> （`docs/plan/report/o3-20260914t181109z.md`、00A D-80），本次在真實 AWS 上**重現了**
> `a2` 與 `a3` 兩個缺口，原樣記錄、不放寬 F49、不刪頁回滾。

## 0. 環境與方法

| 項目 | 值 |
|---|---|
| 帳號／Region | `123456789012` / `us-east-1`（IAM user `tkb-deploy-admin`） |
| 表 | `training_kb`（真實，`RETAIN`） |
| bucket | `training-kb-content-example`（真實） |
| website endpoint | `http://training-kb-content-example.s3-website-us-east-1.amazonaws.com`（**只有 HTTP，S3 website 不提供 HTTPS**） |
| state machine | `arn:aws:states:us-east-1:123456789012:stateMachine:training-kb-ticket-analysis` |
| 注入方式 | **本機程序**設 `TKB_ENV=demo TKB_FAULT=<切點>`，用 library 程式（`content.create_version`／`publishing.Publisher`／`ingress.accept_ticket`）對**真實**資源演練。五個切點都在本機程序內判斷，所以不需要重新部署 Lambda（controller 2026-09-14 裁決）。 |
| demo 資料 | 一律 `p59-` 前綴；演練後刪除 **42 筆 item、38 個物件**，殘留 `(無)`／`(無)`（見 §7） |
| 驅動腳本 | 一次性演練腳本放 scratchpad（不進 repo）；可重跑的回歸版是 `tests/integration/test_recovery.py` 的兩條 `@pytest.mark.aws` |
| 模型 | **完全沒有呼叫**（O5 BLOCKED）。切點 1–4 的路徑不需要模型；切點 5 的雲端執行會走到模型節點，見 §5。 |

## 1. 固定欄位總表

| # | 切點 | 注入方式 | execution_arn | status/error/cause（讀取時間） | 注入後 `current_version` | 注入後 `site/` | 重送後 `version_id` | 重送後 `site/` | 模型呼叫數差值 | 結論 |
|---|---|---|---|---|---|---|---|---|---|---|
| 1 | `s3_after_md` | `TKB_FAULT` ＋ 真實 S3／DDB | 不適用（本機 library） | `TransientError: 注入故障切點：s3_after_md`（06:44:35Z） | `p59-cv-share-summary@v1`（不變） | 沒有 v2 物件 | `p59-cv-share-summary@v2`（同號） | v2 私有產物齊全 | 0 | **PASS** |
| 2 | `ddb_after_version` | 同上 | 不適用 | `TransientError: 注入故障切點：ddb_after_version`（06:44:38Z） | v1（不變） | 沒有 v2 物件 | 同號，STEP 邊 4 條 | 同上 | 0 | **PASS** |
| 3 | `publish_before_transact` | 同上 | 不適用 | `TransientError: 注入故障切點：publish_before_transact`（06:43:29Z） | `p59-prepare-meeting@v1`（不變）、`published_at=None` | `index.html`、`v1.html`（沒有 v2） | `p59-prepare-meeting@v2` | 補齊 v2 | 0 | **PASS** |
| 4 | `publish_after_transact_before_site`（單篇 `a2`） | 同上 | 不適用 | `PublishError: …（切點 a2_after_transact_before_site）：注入故障切點：publish_after_transact_before_site`（06:43:38Z） | **`p59-prepare-meeting@v2`（已切換）**、`published_at=2026-09-14T01:00:00Z` | `index.html`、`v1.html`（**沒有 v2**） | `p59-prepare-meeting@v2`（不變） | `v2.html`＋`v2.diff.txt` 補齊 | 0 | **FAIL（O3）＋ 補償有效** |
| 4b | 多篇 `a3_after_first_site_before_second` | monkeypatch `Publisher._promote` 的第 2 篇（`TKB_FAULT` 的五個切點不含「批次內第 j 篇」） | 不適用 | `PublishError: …（切點 a3_after_first_site_before_second）：a3 注入：第 2 篇 promote 前中斷`（06:44:13Z） | A、B **兩篇都已切換成 v2** | **A 有 v2、B 沒有 v2 → partial** | 兩篇版號不變 | 兩篇 v2 都補齊 | 0 | **FAIL（O3，partial 重現）＋ 補償有效** |
| 5 | `start_execution` | `TKB_FAULT` ＋ 真實 Step Functions（Ticket 路徑） | `arn:aws:states:us-east-1:123456789012:execution:training-kb-ticket-analysis:op-ticket-p59-t99228` | `FAILED` / `PipelineFailed` / `以失敗結束，不發布新版本`（06:44:52Z） | 不適用（連版本都還沒建） | 沒有變化 | 不適用 | 沒有變化 | 0（本機）；雲端節點撞 O5 | **PASS（續跑語意）＋ BLOCKED（O5，雲端執行）** |

## 2. 切點 3（`publish_before_transact`，單篇）

```text
例外型別=TransientError 訊息=注入故障切點：publish_before_transact
讀取時間 2026-09-15T06:43:29Z
$ aws dynamodb get-item --consistent-read TUTORIAL#p59-prepare-meeting
TUTORIAL#p59-prepare-meeting	p59-prepare-meeting@v1	None
$ aws dynamodb get-item --consistent-read VERSION#p59-prepare-meeting@v2
VERSION#p59-prepare-meeting@v2	None	None
$ aws s3api list-objects-v2 --prefix site/tutorials/p59-prepare-meeting/
site/tutorials/p59-prepare-meeting/index.html	site/tutorials/p59-prepare-meeting/v1.html
$ curl -i .../site/tutorials/p59-prepare-meeting/v1.html
HTTP/1.1 200 OK
<article data-published="true" data-retired="false" data-slug="p59-prepare-meeting" data-version-id="p59-prepare-meeting@v1">…
$ curl -i .../site/tutorials/p59-prepare-meeting/v2.html
HTTP/1.1 404 Not Found
```

（`get-item` 的三欄依序是 `PK`、`current_version`、`published_at`；`VERSION` item 沒有
`current_version` 欄位所以第二欄是 `None`。）

**結論 PASS**：交易前中斷後兩個欄位皆未變，公開站讀到的仍是 v1 全文，v2 頁 404。

## 3. 切點 4（`a2_after_transact_before_site`，單篇）＋ `resume_publish` 復原

```text
例外型別=PublishError 訊息=p59-prepare-meeting@v2 已切換 published_at 與 current_version，
但公開頁尚未寫出（切點 a2_after_transact_before_site）：注入故障切點：publish_after_transact_before_site
讀取時間 2026-09-15T06:43:38Z
$ aws dynamodb get-item --consistent-read TUTORIAL#p59-prepare-meeting
TUTORIAL#p59-prepare-meeting	p59-prepare-meeting@v2	None          <- 已切換
$ aws dynamodb get-item --consistent-read VERSION#p59-prepare-meeting@v2
VERSION#p59-prepare-meeting@v2	None	2026-09-14T01:00:00Z          <- published_at 有值
$ aws s3api list-objects-v2 --prefix site/tutorials/p59-prepare-meeting/
site/tutorials/p59-prepare-meeting/index.html	site/tutorials/p59-prepare-meeting/v1.html
$ curl -i .../site/tutorials/p59-prepare-meeting/v1.html   -> HTTP/1.1 200 OK（仍是 v1 全文）
$ curl -i .../site/tutorials/p59-prepare-meeting/v2.html   -> HTTP/1.1 404 Not Found
```

**這就是設計 §18 O3 的缺口本身，在真實 AWS 上重現：DynamoDB 已是新版、公開站整體落後一個
世代。O3 維持 FAIL，不得寫成通過。** 讀者看到的仍是一致的舊版（不是半完成的新版），
所以 F49 沒有被違反，但那是補償不是原子性。

復原（清掉 `TKB_FAULT`，同一個 `operation_id` 呼叫 `resume_publish`）：

```text
resume_publish -> published=('p59-prepare-meeting@v2',) failed=None
預期公開 key = ('site/tutorials/p59-prepare-meeting/v2.html',
               'site/tutorials/p59-prepare-meeting/v2.diff.txt')
讀取時間 2026-09-15T06:43:46Z
$ aws s3api list-objects-v2 --prefix site/tutorials/p59-prepare-meeting/
site/tutorials/p59-prepare-meeting/index.html	site/tutorials/p59-prepare-meeting/v1.html
site/tutorials/p59-prepare-meeting/v2.diff.txt	site/tutorials/p59-prepare-meeting/v2.html
$ curl -i .../site/tutorials/p59-prepare-meeting/v2.html
HTTP/1.1 200 OK
<article data-published="true" data-retired="false" data-slug="p59-prepare-meeting" data-version-id="p59-prepare-meeting@v2">…
頁面含未發布標記？False
ledger：version_id=p59-prepare-meeting@v2 model_output_refs=()
```

**`a2` 復原的順序在真實 AWS 上驗過**：`resume_publish` 先重跑 `Publisher.prepare`
（此時表裡 `published_at` 已有值，渲染出 `data-published="true"`），再
`promote_site_objects`。補出來的頁面**不含** `data-published="false"`，也就是沒有被
`_put_public_object` 的 runtime 守門擋下——這正是 00A §6.7 與上一批 REP §8 第 9 項要求的
硬性順序。版號不變、`model_output_refs` 不變（本次演練沒有模型段，ref 本來就是空 tuple）。

## 4. 多篇 `a3_after_first_site_before_second`（承接 P25 §11）

> **注入方式的說明：** `TKB_FAULT` 的五個切點是固定清單（00A 第 1230 列），不含「批次內第 j
> 篇 promote 之前」。要重現 `a3` 必須讓**第 2 篇**的 promote 失敗，所以這一段沿用 P25 在
> moto 用過的技法：monkeypatch `Publisher._promote`，第一次放行、第二次丟 `TransientError`。
> 產品程式沒有改。

```text
例外型別=PublishError 訊息=p59-b-prepare-meeting@v2、p59-b-share-summary@v2 已切換
published_at 與 current_version，但公開頁尚未寫出（切點 a3_after_first_site_before_second）：
a3 注入：第 2 篇 promote 前中斷
讀取時間 2026-09-15T06:44:13Z
TUTORIAL#p59-b-prepare-meeting	p59-b-prepare-meeting@v2	None
VERSION#p59-b-prepare-meeting@v2	None	2026-09-14T01:00:00Z
TUTORIAL#p59-b-share-summary	p59-b-share-summary@v2	None
VERSION#p59-b-share-summary@v2	None	2026-09-14T01:00:00Z
$ aws s3api list-objects-v2 --prefix site/tutorials/p59-b-prepare-meeting/
… v1.html	v2.diff.txt	v2.html                       <- A 已公開 v2
$ aws s3api list-objects-v2 --prefix site/tutorials/p59-b-share-summary/
… index.html	v1.html                                <- B 還是 v1
$ curl -i .../site/tutorials/p59-b-prepare-meeting/v2.html  -> HTTP/1.1 200 OK
$ curl -i .../site/tutorials/p59-b-share-summary/v2.html    -> HTTP/1.1 404 Not Found
```

**結論 FAIL（O3）：真實 AWS 上重現了 partial publish——同一批裡 A 的 v2 已對外可讀
（HTTP 200）、B 的 v2 還不存在（HTTP 404），違反 F49。** 依 Phase 文件 §9 的停止條件，
本 Phase **不**宣稱發布故障驗收通過、**不**放寬 F49、**不**刪除已 promote 的 A 頁充當回滾
（事後刪除不消除先前曝光）。決策出口仍在維護者手上（00A D-80）。

同 operation 補償重送（讀 `pending-promote.json`）把兩篇補齊：

```text
pending-promote.json = {"version_ids": ["p59-b-prepare-meeting@v2", "p59-b-share-summary@v2"],
 "site_keys": ["site/tutorials/p59-b-prepare-meeting/v2.html",
               "site/tutorials/p59-b-prepare-meeting/v2.diff.txt",
               "site/tutorials/p59-b-share-summary/v2.html",
               "site/tutorials/p59-b-share-summary/v2.diff.txt"]}
resume_publish -> published=('p59-b-prepare-meeting@v2', 'p59-b-share-summary@v2')
讀取時間 2026-09-15T06:44:32Z
$ curl -i .../site/tutorials/p59-b-prepare-meeting/v2.html  -> HTTP/1.1 200 OK
$ curl -i .../site/tutorials/p59-b-share-summary/v2.html    -> HTTP/1.1 200 OK
```

A 那一篇的 v2 已經存在、bytes 相同，`_put_public_object` 的條件寫入（`IfNoneMatch`）撞到
412 之後比對 bytes 一致就靜靜通過——這是真實 S3 條件寫入的行為，不是測試替身。

## 5. 切點 5（`start_execution`，真實 Step Functions）

Ticket 路徑（**Release 走不進 Rote：O6 有 4 列未核定**）。

```text
[注入] TransientError: 注入故障切點：start_execution
[注入後 ledger] status=accepted
  input_ref=operations/op-ticket-p59-t99228/input.json  execution_arn=None
[注入後] 最近的 execution 名稱=['op-ticket-t_p41sm1789448254', 'op-ticket-t_gh-acme-copilot-128',
                               'op-ticket-t_883', 'op-ticket-t_882', 'op-ticket-t_881']
        （沒有 op-ticket-p59-t99228 → 確實沒有啟動任何執行）

[重送] status=duplicate
  execution_arn=arn:aws:states:us-east-1:123456789012:execution:training-kb-ticket-analysis:op-ticket-p59-t99228
[重送] 執行名稱沿用 operation_id？True

$ aws stepfunctions describe-execution（讀取時間 2026-09-15T06:44:52Z）
{
  "executionArn": "arn:aws:states:us-east-1:123456789012:execution:training-kb-ticket-analysis:op-ticket-p59-t99228",
  "name": "op-ticket-p59-t99228",
  "status": "FAILED",
  "error": "PipelineFailed",
  "cause": "以失敗結束，不發布新版本"
}

$ aws stepfunctions get-execution-history（reverseOrder）
[ExecutionFailed]      error=PipelineFailed  cause=以失敗結束，不發布新版本
[LambdaFunctionFailed] error=PermanentError
  cause={"errorMessage": "ValidationException", "errorType": "PermanentError",
         "requestId": "fce6dde3-2ef4-4f8d-9499-095c7ae3c3cf",
         "stackTrace": ["  File \"/var/task/training_kb/pipelines/common.py\", line 200,
           in pipeline_task_handler\n    result: dict[str, JSONValue] = handler(event, context)\n",
          "  File \"/var/task/training_kb/pipelines/ticket.py\", line 788,
           in ticket_analysis_handler\n    return tas…

[第三次重送] status=duplicate
  execution_arn=…:op-ticket-p59-t99228   同一條 execution？True
```

**切點 5 的復原語意 PASS**：注入後 ledger 停在 `accepted`、`input_ref` 已寫、`execution_arn`
仍空（切點插在 `_start_once` **之外**，所以沒有被 `operations.fail(...)` 標成 failed），
雲端確實沒有任何對應的 execution；清掉開關重送走 D-45 的續跑分支，沿用**同一個** execution
name，不換名重跑、不建立第二筆 operation；第三次重送回同一條 execution ARN（`duplicate`
且 `execution_arn` 已有值 → 直接回既有紀錄）。

**雲端執行本身 BLOCKED（O5）**：`EnsureEmbedding` 呼叫 Titan 得到 `ValidationException`，
`BedrockWriter` 轉 `PermanentError`（**不重試**，D-53 第一條 retrier 只接 `TransientError`），
Catch 導向 `PipelineFailed`。這是 O5 未通過的證據（`docs/plan/report/o5-20260915T030245Z.md`），
不是 bug，也不是通過。

## 6. Step Functions console 人工驗收用的 ARN

| 用途 | execution | status / error |
|---|---|---|
| 本 Phase 的 **FAILED**（切點 5 重送後，撞 O5） | `arn:…:execution:training-kb-ticket-analysis:op-ticket-p59-t99228` | `FAILED` / `PipelineFailed`；`LambdaFunctionFailed.error = PermanentError` |
| 對照組 **SUCCEEDED**（P41 的正常流程，引用不重跑） | `arn:…:execution:training-kb-ticket-analysis:op-ticket-t_p41sm1789448254` | `SUCCEEDED`（2026-09-14T21:57:35–38 PDT） |
| 對照組 **FAILED、原因不同**（P41 的 `TKB_FAULT_TASK` 重試耗盡，引用不重跑） | `arn:…:execution:training-kb-ticket-analysis:op-ticket-t_883` | `FAILED` / `PipelineFailed`；`LambdaFunctionFailed.error` 逐字 `TransientError` **三次**（第一次 ＋ D-53 的兩次重試） |

`op-ticket-t_883` 的三次 `TransientError` 是本 Phase 把 `maybe_fail` 改成丟
`TransientError` **本身**（而不是子類 `InjectedFault`）的直接理由：Step Functions 的
`ErrorEquals` 比對 Lambda 回報的**類別名字串**，不認繼承。

> 瀏覽器 console 的逐頁人工檢視需要維護者用自己的 AWS console 登入，agent 這一側以
> `describe-execution` 與 `get-execution-history` 的原文代替，ARN 如上表可直接貼進 console。

## 7. 清理

```text
刪除 42 筆 item、38 個物件；site/index.html 已還原原 bytes
殘留 item：(無)
殘留物件：(無)
```

`site/index.html` 是**共用**的公開物件（P57 發布過一份）。演練的 seed 會覆寫它，所以腳本與
`aws_world` fixture 都在開始前把原 bytes 讀下來、結束時逐字寫回；驗證 ETag 回到 P57 報告記載的
`"f13d0a9936e0515b7ffe48962f64eae4"`（289 bytes），公開站內容與演練前完全相同。

**本次沒有留下任何 `p59-` 殘留。** Step Functions 的 execution 歷史無法刪除（也不該刪），
`op-ticket-p59-t99228` 會永久留在 `training-kb-ticket-analysis` 的歷史裡，作為本紀錄的證據。

---

# 附錄 A — 修正回合 1：Phase 25 §8 的多篇切點 1–4（2026-09-15 08:36–08:41 UTC）

> Phase 59 review Important 2：第一份紀錄只跑了 Phase 25 的**多篇切點 5**（`a3`）。本附錄把
> 剩下的 **多篇切點 1、2、3、4a、4b** 在同一組真實資源上跑完，每一個都「注入 → **同時** curl
> A／B 兩頁 → 復原 → 再 curl A／B」。方法與 §0 完全相同（本機程序 ＋ `TKB_FAULT`／等效注入，
> 對真實表與 bucket 操作），run 前綴改成帶 run id 的 `p59-<6 碼>-`（避免並發撞名）。
>
> **五個多篇切點裡只有切點 5 會產生 partial**，其餘四個都在交易之前或交易本身被擋下，
> A／B 兩頁在注入當下都是 404（＝讀者看到的仍是整批舊狀態）。**O3 維持 FAIL**，理由仍是 §4
> 的切點 5。

| # | 多篇切點（Phase 25 §8 的列） | 注入方式 | 注入後 A／B 的 v2 頁 | 注入後 `current_version` | 復原方式 | 復原後 A／B | 結論 |
|---|---|---|---|---|---|---|---|
| 1 | `assert_batch_publishable` 拒絕（同 slug 兩版同批） | 直接構造 `PublishRequest((A@v2, A@v3))`，不需開關 | **404／404** | 兩篇都 v1；**零 staging** | **不是 resume**：依接受順序拆兩批 | 第一批後 **200／404**（第二批被基底位移擋下） | **PASS** |
| 2 | `prepare` 第 2 篇失敗（關係不完整） | 第 2 篇先用 `TKB_FAULT=ddb_after_version` 停在「沒有 STEP 邊」 | **404／404** | 兩篇都 v1 | 同 operation 重建第 2 篇（沿用版號）再整批發布 | **200／200** | **PASS** |
| 3 | `inspect` 回 `ok=False`（第 2 篇基底在 inspect 前位移） | 把 B 的 `current_version` 改成 `B@v9` | **404／404** | A=v1、B=v9（人為位移） | 修回基底後整批重發 | **200／200** | **PASS** |
| 4a | 交易之前中斷 | `TKB_FAULT=publish_before_transact` | **404／404** | 兩篇都 v1 | **`resume_publish` 沒有輸入**（見下）→ 上游用同一份 `version_ids` 重跑 | **200／200** | **PASS** |
| 4b | 交易**被取消**（基底在 inspect 之後才位移） | `inspect` 換成一律通過 ＋ 位移 B 的基底，只讓 DynamoDB 條件擋 | **404／404** | A=v1、B=v9 | 修回基底後整批重發 | **200／200** | **PASS** |
| 5 | 交易成功、promote 第 2 篇失敗 | monkeypatch `Publisher._promote` 第 2 篇（見 §4） | **200／404 → partial** | 兩篇都已切到 v2 | `resume_publish` 讀 `pending-promote.json` | **200／200** | **FAIL（O3）** |

## A.1 多篇切點 1（`assert_batch_publishable` 拒絕）

```text
同一篇的兩個未發布版本：p59-5acc85-c1-a@v2、p59-5acc85-c1-a@v3
例外型別=PublishError 訊息=同一篇教學不可在同一批出現兩個版本，請依接受順序串行處理
零 staging？True（[]）
TUTORIAL#p59-5acc85-c1-a.current_version = p59-5acc85-c1-a@v1
TUTORIAL#p59-5acc85-c1-b.current_version = p59-5acc85-c1-b@v1
[注入後] 讀取時間 2026-09-15T08:37:10Z
$ curl -i …/site/tutorials/p59-5acc85-c1-a/v2.html   HTTP/1.1 404 Not Found
$ curl -i …/site/tutorials/p59-5acc85-c1-b/v2.html   HTTP/1.1 404 Not Found

復原方式：**不是** resume——依接受順序拆成兩批（O2）。發第一批：
TUTORIAL#p59-5acc85-c1-a.current_version = p59-5acc85-c1-a@v2
[第一批發布後] 讀取時間 2026-09-15T08:37:18Z
$ curl -i …/p59-5acc85-c1-a/v2.html   HTTP/1.1 200 OK   <article data-published="true" …>
$ curl -i …/p59-5acc85-c1-b/v2.html   HTTP/1.1 404 Not Found
第二批：PublishError: 發布前檢查未通過：p59-5acc85-c1-a@v3 的基底已位移：
  current_version 不等於 supersedes（p59-5acc85-c1-a@v2 != p59-5acc85-c1-a@v1）
→ 基底已位移，必須重新配號／重建，這是正確行為（D26）
```

拒絕發生在寫第一個 staging 物件**之前**（`operations/<parent>/` 底下是空的），所以「零
staging、零 DB 變更」是實測到的，不是推論。第二批被 `inspect` 擋下也是預期：A@v3 是以 v1
為基底配出來的，v2 發布之後它的基底已經不成立——**程式不得自己挑較新的那一版**。

## A.2 多篇切點 2（`prepare` 第 2 篇失敗）

```text
第 2 篇建到一半：TransientError: 注入故障切點：ddb_after_version
例外型別=PublishError 訊息=p59-5acc85-c2-b@v2 的內容或關係不完整
TUTORIAL#p59-5acc85-c2-a.current_version = p59-5acc85-c2-a@v1
TUTORIAL#p59-5acc85-c2-b.current_version = p59-5acc85-c2-b@v1
[注入後] 讀取時間 2026-09-15T08:37:39Z
$ curl -i …/p59-5acc85-c2-a/v2.html   HTTP/1.1 404 Not Found
$ curl -i …/p59-5acc85-c2-b/v2.html   HTTP/1.1 404 Not Found

復原：同 operation 重建第 2 篇（沿用版號）再整批發布
[復原後] 讀取時間 2026-09-15T08:37:59Z
TUTORIAL#…-c2-a.current_version = p59-5acc85-c2-a@v2
TUTORIAL#…-c2-b.current_version = p59-5acc85-c2-b@v2
$ curl -i …/p59-5acc85-c2-a/v2.html   HTTP/1.1 200 OK   <article data-published="true" …>
$ curl -i …/p59-5acc85-c2-b/v2.html   HTTP/1.1 200 OK   <article data-published="true" …>
```

這一格同時證明了**建版切點 2 與發布切點的串接**：`ddb_after_version` 留下的「VERSION 有、
STEP 邊 0」正好讓 `prepare` 的 `verify_version_complete` 擋下整批——第 1 篇已經 staging 的
物件留在私有 `operations/`，讀者看不到。

## A.3 多篇切點 3（`inspect` 回 `ok=False`）

```text
inspect.ok=False
  - p59-5acc85-c3-b@v2 的基底已位移：current_version 不等於 supersedes
    （p59-5acc85-c3-b@v9 != p59-5acc85-c3-b@v1）
例外型別=PublishError 訊息=發布前檢查未通過：…（同上）
TUTORIAL#…-c3-a.current_version = p59-5acc85-c3-a@v1
TUTORIAL#…-c3-b.current_version = p59-5acc85-c3-b@v9
[注入後] 讀取時間 2026-09-15T08:38:23Z
$ curl -i …/p59-5acc85-c3-a/v2.html   HTTP/1.1 404 Not Found
$ curl -i …/p59-5acc85-c3-b/v2.html   HTTP/1.1 404 Not Found

復原：把位移的基底修回去（等同上游重讀基底），再整批發布
[復原後] 讀取時間 2026-09-15T08:38:39Z   兩篇 current_version 都是 @v2
$ curl -i …/p59-5acc85-c3-a/v2.html   HTTP/1.1 200 OK
$ curl -i …/p59-5acc85-c3-b/v2.html   HTTP/1.1 200 OK
```

`commit` **完全沒有送出交易**（`inspect` 不過就丟 `PublishError`）；第 1 篇也沒有被單獨發布。

## A.4 多篇切點 4a（交易之前中斷）與 `resume_publish` 的已知邊界

```text
例外型別=TransientError 訊息=注入故障切點：publish_before_transact
TUTORIAL#p59-01c76f-c4a-a.current_version = p59-01c76f-c4a-a@v1
TUTORIAL#p59-01c76f-c4a-b.current_version = p59-01c76f-c4a-b@v1
[注入後] 讀取時間 2026-09-15T08:40:18Z
$ curl -i …/p59-01c76f-c4a-a/v2.html   HTTP/1.1 404 Not Found
$ curl -i …/p59-01c76f-c4a-b/v2.html   HTTP/1.1 404 Not Found

復原：先示範 resume_publish 在**多篇 ＋ 交易前**沒有輸入（D-59 的直接後果）
CoordinationError: 沒有可沿用的操作紀錄：op-release-p59-01c76f-c4a-p
→ 正確的復原方式：上游用**同一份 version_ids** 重跑 prepare／commit
[復原後] 讀取時間 2026-09-15T08:40:33Z   兩篇 current_version 都是 @v2
$ curl -i …/p59-01c76f-c4a-a/v2.html   HTTP/1.1 200 OK
$ curl -i …/p59-01c76f-c4a-b/v2.html   HTTP/1.1 200 OK
```

**本次演練發現的邊界（不是 bug，是設計的直接後果，照實記錄）：**
多篇整批在**交易之前**中斷時，`resume_publish` **拿不到**要補什麼——`pending-promote.json`
是交易成功之後才寫的，而父 operation 依 **D-59 不持有版號**。所以它丟
`CoordinationError: 沒有可沿用的操作紀錄`，**這是正確的**（沒有輸入就不該猜）。這一格的復原
方式是上游用同一份 `version_ids` 重跑 `prepare`／`commit`：版本都還沒發布，走正常路徑即可。
單篇沒有這個問題（`allocate_version` 已把版號寫進同一筆 ledger）。已加回歸測試
`tests/integration/test_recovery.py::test_batch_before_transaction_has_no_resume_input`。

## A.5 多篇切點 4b（交易被取消）

```text
PublishResult: published=() failed=p59-01c76f-c4b-b@v2 reasons=('current_version 不等於 supersedes',)
TUTORIAL#p59-01c76f-c4b-a.current_version = p59-01c76f-c4b-a@v1
TUTORIAL#p59-01c76f-c4b-b.current_version = p59-01c76f-c4b-b@v9
[注入後] 讀取時間 2026-09-15T08:40:56Z
$ curl -i …/p59-01c76f-c4b-a/v2.html   HTTP/1.1 404 Not Found
$ curl -i …/p59-01c76f-c4b-b/v2.html   HTTP/1.1 404 Not Found

復原：修回基底後整批重發（交易沒提交，走正常 commit）
[復原後] 讀取時間 2026-09-15T08:41:11Z   兩篇 current_version 都是 @v2
$ curl -i …/p59-01c76f-c4b-a/v2.html   HTTP/1.1 200 OK
$ curl -i …/p59-01c76f-c4b-b/v2.html   HTTP/1.1 200 OK
```

**條件不符不是例外而是回傳值**：真實 `TransactWriteItems` 整批取消，`failed` 指出第 2 篇、
`reasons` 是 `current_version 不等於 supersedes`，`published` 是空 tuple。第 1 篇**也沒有**被
切換——DynamoDB 的 all-or-nothing 在真表上成立。

## A.6 清理

```text
（第一趟：切點 1–3 ＋ 4a 的第一版）
刪除 84 筆 item、76 個物件；site/index.html 已還原原 bytes
（第二趟：切點 4a 重跑 ＋ 4b）
刪除 42 筆 item、42 個物件；site/index.html 已還原原 bytes
殘留 p59- item：(無)
殘留 p59- 物件：(無)
site/index.html ETag = "f13d0a9936e0515b7ffe48962f64eae4"     <- 與 P57 報告記載的逐字相同
```

本附錄的演練**沒有**讓 seed 去寫共用的 `site/index.html`（review Minor：真實 bucket 上
`World.seed_site_index=False`）；`commit`／`resume_publish` 仍會依正常步驟從真表重建它，
所以 save/restore 的保險留著，結束時 ETag 回到 P57 那一份。
