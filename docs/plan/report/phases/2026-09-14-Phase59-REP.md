# Phase 59：失敗復原與重送驗收 — 實作報告（2026-09-14／15）

> **一句話結論：** 五個 `TKB_FAULT` 切點都接進真實路徑並在**真實 `us-east-1`** 上演練過，
> 同 `operation_id` 的補償重送（`resume_publish`）在真實 S3／DynamoDB 上把中斷的發布補完；
> **但 O3 仍是 FAIL**——多篇整批在 `a3` 切點於真實 AWS 上**重現了 partial publish**
> （A 的 v2 HTTP 200、B 的 v2 HTTP 404），依 Phase 文件 §9 的停止條件原樣記錄、不放寬 F49。
> 演練紀錄：`docs/plan/report/recovery-20260915-0644.md`。

---

## 1. 實作內容與邏輯（逐檔說明）

### 1.1 `src/training_kb/faults.py`（擴充，P41 建的第一片）

P41 已經建好 `FAULT_POINTS`（五個）、`active_fault`、`maybe_fail` 與 `InjectedFault`，
本 Phase **擴充不重建**，只改一件事：

| 改動 | 為什麼 |
|---|---|
| `InjectedFault(TransientError)` 類別 → `InjectedFault = TransientError` **相容別名** | Step Functions 的 `ErrorEquals` 比對 Lambda 回報的**類別名字串**、不認繼承，丟子類就命不中 D-53 第一條 retrier（P41 §9 第 3 點的實證，controller 2026-09-14 裁決） |
| `maybe_fail` 由 `raise InjectedFault(...)` → `raise TransientError(...)` | 同上；訊息仍帶切點名稱，`describe-execution` 的 `cause` 看得出是哪一個切點 |

00A 第 1230 列的名稱**沒有變**：`InjectedFault` 仍可 import、仍可 `except InjectedFault`
或 `pytest.raises(InjectedFault)`，只是它現在指向 `TransientError` 本身。`active_fault`
的兩道保險（`TKB_ENV=prod` 一律回 `None`、打錯的切點名丟 `PermanentError`）原封不動。

對照證據：P41 留下的 `op-ticket-t_883` 執行，`LambdaFunctionFailed.error` 逐字是
`TransientError` **三次**（第一次 ＋ D-53 的兩次重試）；同一條路徑若丟 `InjectedFault`
就只會有一次然後直接進 Catch。

### 1.2 五個切點的插入（`content.py`／`publishing.py`／`ingress.py`）

各檔只加一行呼叫（外加一行註解），位置逐字照 Phase 文件 §7 Task 1 Step 4 的表：

| 切點 | 位置 | 插在這裡的理由 |
|---|---|---|
| 1 `s3_after_md` | `content.create_version`：`put_private_artifact(md_key)` 之後、寫 diff 之前 | 對應矩陣「md 有、diff 無」 |
| 2 `ddb_after_version` | `content.create_version`：`if existing is None: put_meta(planned)` **整個 if 之後**、`_write_edges` 之前 | `put_meta` 只在首次執行；插在 if 之後重送照樣生效，矩陣「私有產物齊全、沒有 STEP 邊」成立 |
| 3 `publish_before_transact` | `Publisher.commit`：`inspect` 通過、`build_commit_transaction` 之前（**在 `_after_transaction` 的 try 之外**） | 例外原樣是 `TransientError`，矩陣「全舊」 |
| 4 `publish_after_transact_before_site` | `Publisher._publish_site`：`_record_pending_promote` **之後**、第一次 `_restage` 之前 | 單篇的 `record_version` 已跑完、多篇的 `pending-promote.json` 已寫好，**兩種復原輸入都在**（P25 review 必修 A1）。例外被 `_after_transaction` 轉成 `PublishError`，訊息帶 `_cut_point` 代號 |
| 5 `start_execution` | `ingress._accept`：`assert_time_left(step="start-execution")` 之後、`_start_once` 之前 | 插進 `_start_once` 會被它的 `except Exception` 攔到並先 `operations.fail(...)` 把 ledger 標 failed；插在外面則 ledger 維持 `accepted`、`input_ref` 已寫、`execution_arn` 仍空，重送剛好走 D-45 的續跑分支 |

「每個切點名稱恰好一處 `maybe_fail(...)`」由 `tests/unit/test_faults.py` 走 **AST** 掃描
斷言（不是字串比對，所以文件字串裡提到切點名不會被誤數）。

### 1.3 `src/training_kb/publishing.py` 的 `# ---- Phase 59 ----` 區段

新增 `_string_tuple`、`_resume_targets`、`resume_publish` 三個名稱，既有程式一行未改
（除了切點 3、4 各一行呼叫）。

```text
resume_publish(operation_id, *, operations, repository, publisher, now) -> PublishResult
  |
  +-- _resume_targets：待補清單怎麼算（兩種輸入互補，00A §6.7）
  |     多篇  operations/<op>/pending-promote.json     <- Publisher._record_pending_promote
  |     單篇  ledger 的 OPS#<op>.version_id + public_site_keys(...)
  |     兩個都沒有 -> CoordinationError（這筆 operation 還沒走到交易，沒東西可補）
  |
  +-- 逐篇 get_version；不存在 -> CoordinationError
  |
  +-- publisher.prepare(...)          <- **硬性順序：先 prepare**
  |
  +-- 有任何一篇 published_at is None -> publisher.commit(...)   # 切點 1、2、3、5
  |
  +-- 全部已發布 -> promote_site_objects(...)                     # 切點 4
        |
        +-- 實際補出的 key != 待補清單 -> CoordinationError（兩份真相分岔了）
        +-- 重建教學索引 + 站台索引，回 PublishResult(published=version_ids)
```

**為什麼一定要先 `prepare`**（00A §6.7、上一批 REP §8 第 9 項）：切點 4 之前留下的 staging
是交易**前**渲染的，那時 `published_at` 還是 `None`，頁面帶著 `UNPUBLISHED_MARKER`
（`data-published="false"`）；直接 promote 會被 `_put_public_object` 的 runtime 守門擋下並丟
`PublishError`——擋得好，但那代表**復原失敗**而不是成功。重跑 `prepare` 時表裡的
`published_at` 已經有值，重新渲染出來的 staging 才是 `data-published="true"`。
真實 AWS 上驗過：復原後的 `v2.html` HTTP 200 且**不含**未發布標記（recovery 報告 §3）。

**本計畫選擇：補完版本頁與 diff 副本之後接著重建兩層索引。** 00A 第 1230 列說
`resume_publish` 要「把中斷的發布接著做完」，而 `_publish_site` 的第 4、5 步就是索引；
只補版本頁會讓公開站永遠沒有連到新版的路徑（§2 的不變量說注入後索引不得有 v2 連結，
復原就必須把它補回去）。索引是可重建投影（`if_none_match=False`），不影響
「補出的 key 清單 == 待補清單」這條斷言。

`resume_publish` **不**呼叫 `Publisher.commit`（已發布時 `inspect` 會擋下）、**不**刪除既有
公開物件、**不**重新 `allocate_version`。

### 1.4 `infra/scripts/check_asl.py`（新增）

`LAMBDA_SERVICE_ERRORS`、`iter_task_states`、`check_asl_document`、`main`，另加
`ASL_ROOT`／`ASL_GLOB`／`NESTED_KEYS`／`_expected`。它**包裝** P29 的 `assert_safe_asl`
（不重寫它的 `StartAt`／`Choice.Default`／Catch 目標同層這些結構規則），自己補三件
`assert_safe_asl` 做不到的事：

1. **收齊全部問題**。`assert_safe_asl` 第一個問題就丟 `PermanentError`；本函式走完所有 Task
   （含 `Map` 的 `ItemProcessor`／`Iterator` 與 `Parallel` 的 `Branches`）再把它的訊息追加成
   最後一筆。
2. **逐字比對 D-53 第二條 retrier**。`assert_safe_asl` 只說「前 2 條 Retry 不是固定參數」，
   本檢查另外指名 `Lambda.ServiceException`，看訊息就知道少的是哪一類。
3. **Catch 的終點名稱**必須就是 `PipelineFailed`（`assert_safe_asl` 只要求同層任一 `Fail`）。

`main(argv)`：給路徑就檢查那幾份，不給就掃 `infra/stepfunctions/*/v*.json`。
**掃不到檔案回 2 並印「找不到 ASL 定義」**（本計畫選擇：與「掃到但不通過」的 1 分開，
呼叫端分得出是缺檔還是檢查沒過），絕不靜靜回 0。

### 1.5 測試檔

| 檔 | 內容 |
|---|---|
| `tests/unit/test_faults.py`（擴充） | P41 那 6 條保留；改寫「`InjectedFault` 是子類」那條成「注入丟的類別名逐字是 `TransientError`」，新增 AST 掃描的兩條（每個切點恰好一次、落在預期的檔案） |
| `tests/unit/test_check_asl.py`（新增，11 條） | 逐條檢查、`Map`／`Parallel` 走訪、CLI 的四種結果（三份全通過／缺檔／刪 Catch／刪第二條 retrier） |
| `tests/integration/test_recovery.py`（新增，22 條：20 moto ＋ 2 `aws`） | 切點矩陣（5 條 parametrize）、O3 缺口單獨斷言、整批不切換、整批復原讀 `pending-promote.json`、補償重送、四個早期切點的收斂（4 條 parametrize）、同事件重送不加樣本、切點 5 的續跑、closed execution、lease 串行；另兩條 `@pytest.mark.aws` 對真實資源跑切點 1–4 與復原 |

`World` 是測試門面：`deliver_release` 依序走
`ingress.accept_release` → 模型段替身 → `allocate_version`／`create_version` →
`Publisher.prepare`／`commit`，五個切點全在這條線上。**本計畫選擇：不經
`run_release_update`**（它需要 Bedrock，O5 BLOCKED，接上去只會讓每個切點都先撞模型）。
模型段替身：ledger 已有 `model_output_refs` 就完全不呼叫，所以「重送期間模型呼叫數為 0」
是真的被觀察到，不是被繞過。

---

## 2. 步驟

1. 讀 `IMPLEMENTER.md` → `COMMON.md` → `briefs/P59-brief.md` → Phase 59 文件 → P41／P48／P52／P57 報告的相關段落。
2. 跑基線：`uv run pytest tests -q -W error` → **1455 passed / 45 skipped / 11 xfailed**（COMMON.md 記的 924 是更早的快照，同波次其他 Phase 已經加了測試）。
3. Task 1：紅燈 → 改 `faults.py` ＋ 插五個切點 → 綠燈 → commit `238bd38`。
4. Task 2：紅燈 → 寫 `resume_publish` → 綠燈 → commit `22047b5`。
5. Task 3：紅燈 → 寫 `check_asl.py` → 綠燈 ＋ 三份 ASL 全 `[通過]` → commit `d51b735`。
6. Task 3 Step 4A：真實 AWS 演練（`aws` marker 測試 ＋ 一次性演練腳本），證據寫進 `docs/plan/report/recovery-20260915-0644.md`，清理 demo 資料並還原 `site/index.html`。
7. 勾 Phase 文件 §7／§11、補「本計畫選擇」、寫本報告 → commit。

---

## 3. 逐 Task 的 TDD 紅燈／綠燈

### Task 1：`faults.py` 的五個切點與環境限制

**RED**

```bash
$ uv run pytest tests/unit/test_faults.py -q
...
>       assert sorted(wired) == sorted(FAULT_POINTS)
E       AssertionError: assert [] == ['ddb_after_v...rt_execution']
E         Right contains 5 more items, first extra item: 'ddb_after_version'
...
FAILED tests/unit/test_faults.py::test_injected_fault_is_transient_error_itself
FAILED tests/unit/test_faults.py::test_every_fault_point_is_wired_exactly_once
FAILED tests/unit/test_faults.py::test_fault_points_live_in_the_expected_files
3 failed, 6 passed in 0.21s
```

（紅燈訊號不是 brief 預估的 `ImportError`：P41 已經建好模組與四個名稱，缺的是「切點沒接進
程式」與「注入型別要換成 `TransientError` 本身」，所以紅燈落在斷言而不是 import。）

**GREEN**

```bash
$ uv run pytest tests/unit/test_faults.py -q
9 passed in 0.19s
$ uv run pytest tests/unit tests/integration -q
1466 passed, 45 skipped, 11 xfailed in 285.72s
```

### Task 2：切點矩陣、整批不切換與同 operation 重送

**RED**

```bash
$ uv run pytest tests/integration/test_recovery.py -q
tests/integration/test_recovery.py:64: in <module>
    from training_kb.publishing import (
E   ImportError: cannot import name 'resume_publish' from 'training_kb.publishing'
1 error in 0.25s
```

**GREEN**

```bash
$ uv run pytest tests/integration/test_recovery.py -q
20 passed, 2 skipped in 14.20s        # 2 skipped = 兩條 aws marker
```

中途兩次紅燈（都是測試本身寫錯，不是產品程式）：

1. `test_same_event_resend_adds_no_samples` 撞 `ContentError: 版本 … 已發布，不可覆寫`
   —— `World.deliver_release` 少了 §6 的「`status == "done"` 就取既有結果」分支，補上後綠燈。
2. `test_resend_after_early_cut_points_converges[start_execution]` 斷言 `model_calls` 不變而
   得到 `assert 1 == 0` —— 切點 5 在模型段**之前**，那次重送模型本來就是第一次跑；
   改成「切點 5 ＋1、切點 1／2／3 為 0」並在 docstring 寫明理由。

### Task 3：ASL 靜態檢查

**RED**

```bash
$ uv run pytest tests/unit/test_check_asl.py -q
E   ModuleNotFoundError: No module named 'infra.scripts.check_asl'
1 error in 0.14s
```

**GREEN**

```bash
$ uv run pytest tests/unit/test_check_asl.py -q
11 passed in 0.06s

$ uv run python -m infra.scripts.check_asl
[通過] infra/stepfunctions/feedback-review/v1.json
[通過] infra/stepfunctions/release-update/v1.json
[通過] infra/stepfunctions/ticket-analysis/v1.json
exit=0
```

### Task 3 Step 4A：真實 AWS

```bash
$ TKB_RUN_AWS_INTEGRATION=1 TKB_AWS_REGION=us-east-1 TKB_TABLE_NAME=training_kb \
  TKB_CONTENT_BUCKET=training-kb-content-example \
  uv run pytest tests/integration/test_recovery.py -m aws -q
2 passed, 19 deselected in 44.32s
```

完整演練（含 website endpoint 的 HTTP 回應、多篇 `a3` partial、真實 Step Functions 續跑）
在 `docs/plan/report/recovery-20260915-0644.md`。

---

## 4. 測試方式與結果

| 指令 | 結果 |
|---|---|
| `uv run pytest tests -q -W error` | **1526 passed / 49 skipped / 11 xfailed**（基線 1455／45／11；本 Phase ＋42 條，其餘來自同波次其他 Phase） |
| `uv run ruff check src tests infra` | `All checks passed!` |
| `uv run mypy` | `Success: no issues found in 54 source files` |
| `uv run python -m infra.scripts.check_asl` | 三份全 `[通過]`，exit 0 |
| `TKB_RUN_AWS_INTEGRATION=1 … pytest tests/integration/test_recovery.py -m aws -q` | `2 passed, 19 deselected` |

`ruff format --check` **沒跑**在既有檔與共用檔上（COMMON.md：gate 不含它，既有程式沒有經過
`ruff format`，對舊檔會紅）。新建的 `tests/integration/test_recovery.py`、
`tests/unit/test_check_asl.py`、`infra/scripts/check_asl.py` 也維持與專案其他檔一致的手寫
排版，沒有跑 `ruff format`——跑了會讓它們與相鄰檔案的風格分岔。

### 驗收矩陣逐列

| 路徑 | 判定 | 證據 |
|---|---|---|
| Happy：無注入的建版與發布 | **PASS** | `test_resend_after_early_cut_points_converges`（moto）、recovery §3 復原後 HTTP 200 |
| Failure：五個切點逐一注入 | **PASS** | `test_no_public_change_after_injected_fault`（5 條 parametrize）；真實 AWS 見 recovery §2／§3／§5 |
| Failure：多篇整批在任一切點失敗 | **切點 3 PASS；`a3` FAIL（O3）** | moto `test_batch_publish_is_all_or_nothing` 綠；**真實 AWS 在 `a3` 重現 partial**（recovery §4） |
| Boundary：切點 4 注入 | **FAIL（O3）＋ 補償有效** | `test_cut_point_4_is_the_o3_gap`、recovery §3 |
| Boundary：同 operation 重送 | **PASS** | `test_resend_reuses_version_and_model_output`（版號、`model_output_refs` 不變、模型呼叫 0） |
| Boundary：同一事件重送 | **PASS** | `test_same_event_resend_adds_no_samples`（VERSION／FEEDBACK／`success_count` 三者不變） |
| Boundary：同名 execution 已結束 | **PASS** | `test_closed_execution_needs_a_ledger_result`（`status != done` → `CoordinationError`；`complete` 之後才沿用） |

---

## 5. 遇到的問題與解法

1. **`InjectedFault` 在雲端證不出 Retry。** P41 §9 第 3 點已警告。依 controller 裁決把它降級成
   `TransientError` 的相容別名，注入一律丟 `TransientError` 本身；00A 的名稱不變。P41 留下的
   `test_injected_fault_is_a_transient_error`（斷言 `__name__ == "InjectedFault"`）改寫成新契約
   ——那條測試原本就是風險備忘，檔案 owner 是 P59（00A §3.3）。
2. **`aws` 測試把共用的 `site/index.html` 蓋掉了。** 第一次跑 `aws` marker 測試時，seed 會寫
   `site/index.html`（模擬「上一次發布」），結果把 P57 發布的站台索引覆寫成只列 `p59-` 教學。
   發現後：(a) 用 P57 報告記載的原文還原，ETag 回到 `"f13d0a9936e0515b7ffe48962f64eae4"`
   （289 bytes）逐字相同；(b) 在 `aws_world` fixture 與演練腳本都加上「開始前存下原 bytes、
   結束時逐字寫回」。**這是本 Phase 對真實環境造成又修復的唯一一次副作用，照實記錄。**
3. **`TKB_FAULT` 沒有「批次內第 j 篇」的切點。** 五個切點是 00A 的固定清單。要重現 Phase 12 的
   `a3_after_first_site_before_second` 只能讓第 2 篇的 promote 失敗，所以沿用 P25 在 moto 用過
   的技法 monkeypatch `Publisher._promote`（產品程式不動），並在 recovery 報告寫明注入方式。
4. **`resume_publish` 只補版本頁會留下半完成的復原。** 補完之後接著重建教學索引與站台索引，
   理由見 §1.3。
5. **模型呼叫數在切點 5 的期望值。** 切點 5 在模型段之前，重送時模型是第一次跑（＋1）；
   切點 1／2／3 在模型段之後，重送必須是 0。測試分開斷言並寫進 docstring。
6. **mypy strict 涵蓋 `infra/`。** `check_asl.py` 與 `resume_publish` 都補了完整註記；
   `pending-promote.json` 的欄位改用 `_string_tuple` 驗形狀，避免整段 `# type: ignore`。

---

## 6. Phase 文件修改清單

`docs/plan/unfinish/59-Phase59-失敗復原與重送驗收.md`：

1. 前言新增 **（e）本計畫選擇（2026-09-14，實作當下的裁決）** 共 7 點（注入型別、索引重建、
   `World` 不經 `run_release_update`、`a3` 的注入方式、`main` 的回傳碼、真實 AWS 的演練做法、
   P41 測試改寫）。
2. §7 的 16 個 Step checkbox 全部勾起（Task 1／2／3 的 Step 1–5 與 Task 3 Step 4A）。
3. §11 完成清單 13 列：**12 列勾起、1 列維持未勾**——「多篇整批失敗時兩篇同時維持舊狀態，
   沒有 partial publish」旁註明 `FAIL（O3）`，真實 AWS 上重現了 partial，依 §9 停止條件不改寫成
   通過、不放寬 F49，證據指向 recovery 報告 §4。

---

## 7. gate 狀態與給下一個 Phase 的介面清單

| gate | 本 Phase 的結論 |
|---|---|
| **O2** | **仍 PASS**，並在真實 S3／DynamoDB 上再驗一次：切點 1／2 中斷後同 operation 重送拿到**同一個** `p59-cv-share-summary@v2`，沒有 v3、沒有第二筆 operation（recovery §1 第 1、2 列） |
| **O3** | **維持 FAIL。** `a2`（單篇）與 `a3`（多篇 partial）兩個缺口都在真實 AWS 上重現並留下 HTTP 回應原文。補償重送有效 ≠ 原子性，**不得**宣稱「發布故障驗收已通過」，F49 不放寬，已 promote 的頁不刪。決策出口仍在維護者手上（00A D-80） |
| **O5** | **維持 BLOCKED。** 切點 5 重送後的雲端執行 `op-ticket-p59-t99228` 走 `PermanentError（ValidationException）→ Catch → PipelineFailed`，原文在 recovery §5 |
| **O6** | 沒有動 `tests/fixtures/o6/approved-sources.json`；Release 端到端維持 **BLOCKED（O6）**，切點 5 的真實驗證改走 Ticket 路徑 |
| O1／O4／O7 | 與本 Phase 無關 |

**給 P60 的介面清單**

| 名稱 | 值／位置 |
|---|---|
| `training_kb.faults` | `FAULT_POINTS`（五個）、`active_fault`、`maybe_fail`（丟 `TransientError` **本身**）、`InjectedFault`（＝`TransientError` 的相容別名） |
| `publishing.resume_publish` | `resume_publish(operation_id, *, operations, repository, publisher, now) -> PublishResult`；待補清單優先讀 `pending-promote.json`、單篇回退 ledger 版號；補完版本頁後重建兩層索引 |
| `infra/scripts/check_asl.py` | `uv run python -m infra.scripts.check_asl`；exit 0 全通過、1 有問題、**2 掃不到檔**。P60 的部署前檢查可直接串它 |
| 部署前必查的兩個開關 | `TKB_ENV=prod`（正式環境一律不注入）、`TKB_FAULT` **未設**；另有 P41 的 `TKB_FAULT_TASK` 也必須未設。**請 P60 的 `check_secrets`／部署清單列這三項** |
| 演練紀錄 | `docs/plan/report/recovery-20260915-0644.md`（execution ARN、HTTP 回應、清理結果） |
| 真實資源上的殘留 | **無 `p59-` 殘留**（42 筆 item、38 個物件已刪）；`site/index.html` 已還原。Step Functions 歷史裡永久留著 `op-ticket-p59-t99228`（FAILED／O5），那是證據不是垃圾 |
| console 人工驗收 ARN | FAILED：`…:execution:training-kb-ticket-analysis:op-ticket-p59-t99228`；SUCCEEDED（P41 的正常流程）：`…:op-ticket-t_p41sm1789448254`；FAILED 且原因不同（P41 的 `TransientError` 重試耗盡）：`…:op-ticket-t_883` |

---

## 8. commits

| SHA | subject |
|---|---|
| `238bd38` | feat(faults): 以 TKB_FAULT 在五個切點注入失敗 |
| `22047b5` | test(recovery): 切點注入與同 operation 重送 |
| `d51b735` | feat(infra): 檢查 ASL 的兩條 Retry 與 Catch |
| `8b0ff84` | docs(plan): Phase 59 完成勾選與實作報告 |
| `bdf4338` | fix(recovery): 真 AWS 演練還原共用的站台索引（§5 第 2 點的保護，補提交） |

---

## 9. 未完成／疑慮／建議 controller 裁決

1. **O3 的 `a3` partial 在真實 AWS 上重現了（FAIL）。** 這是本 Phase 最重要的產出：同一批裡
   A 的 v2 已對外可讀（HTTP 200）、B 的 v2 還是 404，違反 F49。程式面的補償（`resume_publish`
   讀 `pending-promote.json`）確實能把兩篇補齊，但**在補齊之前讀者已經看到不一致**。
   依 Phase 文件 §9 的停止條件，本 Phase 不宣稱通過、不刪頁回滾。**建議 controller 把這份
   證據併進 O3 的維護者決策材料（00A D-80）**；P60 的端到端完成證據也不得把它寫成通過。
2. **`InjectedFault` 已不是獨立類別。** 00A 第 4.1 節與第 1230 列的敘述句「`InjectedFault`
   繼承 `TransientError`」現在字面上不精確（它**就是** `TransientError`）。名稱契約沒破，
   但**建議 controller 讓 doc sync 把那兩處改成「`InjectedFault` 是 `TransientError` 的相容
   別名；注入一律丟 `TransientError` 本身，雲端 `ErrorEquals` 才比得中」**。
3. **`resume_publish` 多做了索引重建**（§1.3 的本計畫選擇）。00A 第 1230 列只說「把中斷的發布
   接著做完」，沒有逐條列步驟；若 controller 認為索引重建不該在復原路徑上，拔掉那三行即可，
   其餘斷言不受影響。
4. **`aws` 測試會動到共用的 `site/index.html`。** 已加「存下原 bytes → 結束寫回」的保護，但
   **兩個 Phase 同時跑真實 AWS 測試時仍可能互相覆寫**。建議 controller 規定同一時間只有一個
   Phase 跑真實 bucket 的寫入測試，或請 P60 在收尾時重建一次站台索引。
5. **Step Functions console 的逐頁人工檢視**只留了 ARN 與 CLI 原文（agent 沒有 console 登入
   session）。維護者要人工確認時，recovery §6 的三個 ARN 可直接貼進 console。
6. **`World.deliver_release` 不經 `run_release_update`**（§1.5 的本計畫選擇）。等 O5 解除之後，
   建議由 P60 或後續 Phase 把同一組切點在**完整 pipeline**上再跑一次，才算涵蓋
   「pipeline 內部節點中斷」這一類；本 Phase 的五個切點都是 library 層的。
7. **模型輸出重用的證據只在 moto**（真實 AWS 的演練沒有模型段，`model_output_refs` 是空
   tuple）。這是 O5 BLOCKED 的直接後果，不是漏做；O5 解除後才補得起來。
