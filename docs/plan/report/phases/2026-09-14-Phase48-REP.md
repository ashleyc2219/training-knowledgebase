# Phase 48 實作報告：Feedback Review 排程流程

> 對象：沒看過實作過程的人。region 一律 `us-east-1`，帳號 `123456789012`。
> **O5 BLOCKED、O3 FAIL** 的地方一律標明，沒有把「跑不了」寫成「通過」。

---

## 1. 實作內容與邏輯（逐檔說明）

### 1.1 `src/training_kb/pipelines/feedback.py`（追加 `# ---- Phase 48 ----` 區段）

這支檔的 owner 是 Phase 44，P45／P46／P47 各自有區段。本 Phase **只追加自己的區段**，
一個字都沒有改別人的程式（只在檔頭的 import 區加了三行：`os`、`load_settings`、
`pipelines.common` 與 `publishing`／`site` 的匯入）。

| 名稱 | 做什麼 |
|---|---|
| `FEEDBACK_REVIEW_PIPELINE` | `"feedback-review"`，`run_sequence` 與 ASL 的 `Parameters.pipeline` 都用它 |
| `REVIEW_MODES` | `("formal", "demo")`，ASL input 唯一合法的兩個值 |
| `_review_mode(state)` | 讀 `mode`；未知值丟 `PermanentError`（**不默默退回 formal**）。回傳值逐一比對 `REVIEW_MODES`，型別收斂靠比對本身，不用 `cast` |
| `_ids` / `_listed` | `list[str]` 與 `list[JSONValue]` 的互轉。list 在型別上是不變的，`list[str]` 不是 `list[JSONValue]` 的子型別，直接塞進 state 過不了 `mypy --strict`；只在這裡轉一次，不在每個 Task 各寫一份 `cast` |
| `_review_targets(repository)` | active Tutorial 的 current 且已發布的版本 ID，升序 |
| `review_operation_id(state, deps)` | `op-feedback-review-<project>-<UTC 日期>`；canonical id 用**連字號**（D-61），`execution_name` 才不必走 SHA-256 截取 |
| `task_list_targets` | 先驗 `mode`（未知就連表都不掃）→ `accept` 當日 operation → 列目標 |
| `task_evaluate_targets` | 同一個 Task 裡兩條**獨立**分支；每個弱教學 target 先 `accept` 一筆 `refine_operation_id` 子 operation（D-59）再交給 `diagnose_weak`／`prepare_refine`；逐篇「沒有改動」的理由寫私有 `review-no-change.json`，**不進 state** |
| `_publisher` / `_prepared` | `Publisher(repository, SiteRenderer(), operations)`；`_prepared` 每個 Task 重算一次（同 operation 的 staging 是決定性的，所以不必把 `PreparedPublish` 塞進 state），超過 `MAX_BATCH_VERSIONS` 在寫第一個 staging 之前就擋下來 |
| `task_prepare_batch` | 只寫私有 staging ＋ `operations/<op>/publish-request.json`；結束時**一個公開物件都還沒寫** |
| `task_inspect_batch` | 整批只讀不寫；任一篇不過丟 `PermanentError` |
| `task_commit_batch` | 一次交易切完整批；`PublishResult.failed` 有值就丟 `PermanentError`（**不回一個看起來正常的結果**，比照 P41 review 必修 3）。最後寫 `review-result.json`（把 `review-no-change.json` 併進來）並 `operations.complete(...)` ——這是整條流程的停止點 |
| `FEEDBACK_REVIEW_TASKS` / `_TASK_BY_NAME` / `_DEPS` | 五個 Task 的固定順序；名稱由 `task_name` 導出，不另打字串表 |
| `run_feedback_review` | 本機整條序列：先補 `operation_id` 再交給 `run_sequence`（D-07 的第一個參數是 pipeline 名） |
| `feedback_review_handler` | 共用 Lambda 的直接入口，**只跑 `event["task"]` 那一個 Task**，不 try／except |

**為什麼是三個 Task 而不是一個。** `prepare`／`inspect`／`commit` 本來就是 P24／P25 的三段式；
本 Phase 唯一多做的事是把它們**拆成三個 ASL Task**，這樣「第二篇檢查失敗」一定發生在任何
公開前綴寫入**之前**。拆進 `Map` 逐篇處理會讓「第一篇已 publish、第二篇失敗」變成可能，
那正是 F49 禁止的形狀（§3 有實驗證據）。

### 1.2 `infra/stepfunctions/feedback-review/v1.json`（新檔）

五個 Task 一條直線 `ListTargets → EvaluateTargets → PrepareBatch → InspectBatch →
CommitBatch → Succeeded`，**沒有 Choice、沒有 Map**，加上 `Succeeded`（Succeed）與
`PipelineFailed`（Fail）兩個終點。每個 Task 的 `Retry`／`Catch`／`TimeoutSeconds` 都是
`task_state(ARN, next)` 的輸出（Phase 29 的 `RETRY`／`CATCH`），本 Phase 沒有抄任何字面值；
`Parameters` 封套只有 `pipeline`／`task`／`state.$`，**沒有** `Payload` 外層（D-24、D-49）。

檔案是用 `canonical_json(definition)` 的 bytes 落檔（`indent=2, sort_keys=True`），所以
`DefinitionBody.from_file` 讀到的 bytes 與 `save_asl_snapshot` 存進 S3 的**逐 byte 相同**。

### 1.3 `infra/training_kb_stack.py`（追加 `# ---- Phase 48 ----` 區段）

模組層加 `REVIEW_MACHINE`／`REVIEW_ASL_PATH`／`REVIEW_SCHEDULE_NAME`／
`REVIEW_SCHEDULE_EXPRESSION`／`REVIEW_SCHEDULE_TIMEZONE`／`REVIEW_SCHEDULE_INPUT`；
建構子加五個 construct：

1. `FeedbackReviewLogs`（`/aws/vendedlogs/states/training-kb-feedback-review`，90 天）
2. `FeedbackReview`（Standard、`DefinitionBody.from_file`、
   `definition_substitutions={"PipelineTaskFunctionArn": self.task_function.function_arn}`、
   `timeout=1 小時`）
3. `self.task_function.grant_invoke(self.feedback_review)`
4. `ReviewSchedulerRole`（`scheduler.amazonaws.com`）＋
   `self.feedback_review.grant_start_execution(...)`——**只**這一條 state machine
5. `DailyFeedbackReview`（`scheduler.CfnSchedule`，`cron(30 0 * * ? *)`、`UTC`、
   `FlexibleTimeWindow OFF`、`input={"mode": "formal"}`）

**本 Phase 不新增任何 Lambda**（D-23：三條 pipeline 共用 `training-kb-pipeline-task`），
相依 layer 沿用 P41 的 `self.deps_layer`（R2）。也**沒有**給 webhook
`grant_start_execution`／`DescribeExecution`：`feedback-review` 沒有外部事件入口。

### 1.4 測試

- `tests/unit/test_feedback_review_flow.py`（22 條）：兩條分支、state 契約、整批語意、
  handler、ASL 結構、CDK template 與排程。器材用 moto 表＋bucket 上的**真** `Repository`
  與**真** `OperationCoordinator`（見 §5 第 1 點）。
- `tests/integration/test_feedback_review_state_machine.py`（6 條，`@pytest.mark.aws`）：
  部署定義／S3 快照／`get-schedule`／三條真實 execution。每一組都自足
  （seed → start → 等終態 → 斷言 → **清理**），`TKB_CONTENT_BUCKET` 沒設時整支 skip。
- `tests/unit/pipelines/test_ticket_flow.py`：改了**一條**測試（見 §5 第 4 點），P41 的
  docstring 本來就寫「由 P48 改掉」。

---

## 2. 步驟

1. 讀 IMPLEMENTER.md → COMMON.md → P48 brief → Phase 48 文件 → P41 §7 與修正回合 1 →
   P44／P45／P46／P47 §7。
2. 確認前置：`infra/training_kb_stack.py`、`infra/stepfunctions/`、`task_name`／
   `build_deps`／`pipeline_task_handler`、`training-kb-pipeline-task` 與 `build/lambda-layer/`
   都在（P41 已合併）。
3. Task 1 → Task 2 → Task 3，各自先紅燈再綠燈，一個 Task 一個 commit。
4. `uv run python -m infra.scripts.build_lambda_layer` → `cdk synth` → `cdk deploy` →
   `save_asl_snapshot` → 三條真實 execution → `aws scheduler get-schedule`。
5. `uv run ruff check src tests infra`、`uv run mypy`、`uv run pytest tests -q -W error`。
6. 勾 Phase 文件 §7／§11，補 §12「實作結果」，寫本報告。

部署指令（`--outputs-file` 指到 scratchpad，**沒有**提交 `cdk.out/`）：

```bash
uv run python -m infra.scripts.build_lambda_layer
export TKB_CONTENT_BUCKET=training-kb-content-example
export TKB_GITHUB_WEBHOOK_SECRET=…   # 從已部署的 training-kb-webhook 讀回來，不進 repo／報告
AWS_REGION=us-east-1 AWS_DEFAULT_REGION=us-east-1 command npx aws-cdk@2 synth TrainingKbApp --quiet
AWS_REGION=us-east-1 AWS_DEFAULT_REGION=us-east-1 command npx aws-cdk@2 deploy TrainingKbApp \
  --exclusively --require-approval never --outputs-file "$SCRATCH/cdk-outputs-p48.json"
```

---

## 3. 逐 Task 的 TDD 紅燈／綠燈

### Task 1：兩條獨立分支與 state 契約

**紅燈** `uv run pytest tests/unit/test_feedback_review_flow.py -q`

```text
tests/unit/test_feedback_review_flow.py:53: in <module>
    from training_kb.pipelines.feedback import (
E   ImportError: cannot import name 'FEEDBACK_REVIEW_PIPELINE' from
    'training_kb.pipelines.feedback' (~/AWS-Hackathon/src/training_kb/pipelines/feedback.py)
1 error in 0.32s
```

（brief §4 寫的是 `FEEDBACK_REVIEW_TASKS`；那個名稱要等五個 Task 都存在才生得出來，是
Task 2 的紅燈。Task 1 的測試只 import 它真的交付的名稱——見 Phase 文件 §12 本計畫選擇 2。）

**綠燈** 同一條指令 → `8 passed in 8.13s`

八條：`review_operation_id` 的形狀、未知 mode `PermanentError`、只取 active 已發布、
兩條分支互不阻擋、`review-no-change.json` 不進 state、每個弱教學一筆子 operation、
REFINE 失敗仍保住 candidate、state 不帶留言或全文。

**commit** `19ab65d feat(feedback): 組合每日回饋檢視的兩條獨立分支`

### Task 2：整批 prepare／inspect／commit 與 F49

**紅燈** 同一條指令

```text
tests/unit/test_feedback_review_flow.py:53: in <module>
    from training_kb.pipelines.feedback import (
E   ImportError: cannot import name 'FEEDBACK_REVIEW_TASKS' from 'training_kb.pipelines.feedback'
1 error in 0.33s
```

**綠燈** 同一條指令 → `16 passed in 27.31s`

**Phase 文件 §7 Task 2 Step 4 要求的手動實驗（逐篇 commit 必須讓測試轉紅）。**
把 `task_inspect_batch` 改成直接回傳、`task_commit_batch` 改成 `for version_id in ...:
publisher.commit(publisher.prepare(PublishRequest((version_id,), op)))` 逐篇提交，重跑兩條測試：

```text
FAILED tests/unit/test_feedback_review_flow.py::test_two_prepared_versions_commit_together
FAILED tests/unit/test_feedback_review_flow.py::test_second_version_failing_inspection_publishes_nothing
2 failed, 14 deselected in 7.71s

# test_second_version_failing_inspection_publishes_nothing（本來要證「零篇公開」）
E   training_kb.errors.PublishError: 發布前檢查未通過：c@v2 的基底已位移：
    current_version 不等於 supersedes（c@v9 != c@v1）
    ← b@v2 這時**已經被 commit 出去了**，只有 c 失敗 = 部分發布

# test_two_prepared_versions_commit_together（順帶暴露第二個問題）
E   training_kb.errors.CoordinationError: operation op-feedback-review-demo-2026-09-13
    already has version b@v2: c@v2
E   training_kb.errors.PublishError: c@v2 已切換 published_at 與 current_version，
    但公開頁尚未寫出（切點 a2_after_transact_before_site）：…
```

逐篇 commit 不只違反 F49，還會在 `record_version` 撞上「一個 operation 只能有一個版號」。
確認之後改回整批版本，`16 passed` 恢復。實驗原文存在 scratchpad
`f49-per-version-experiment.txt`（不入 repo）。

**commit** `ea9cde7 feat(feedback): 整批 prepare/inspect/commit 與零部分發布`

### Task 3：ASL、每日排程與雲端驗收

**紅燈** 同一條指令

```text
tests/unit/test_feedback_review_flow.py:535: in <module>
    ASL_PATH = PROJECT_ROOT / ASL_LOCAL_PATH.format(pipeline="feedback-review", number=1)
…
E   FileNotFoundError: [Errno 2] No such file or directory:
    '~/AWS-Hackathon/infra/stepfunctions/feedback-review/v1.json'
3 failed, 16 passed, 3 errors in 23.30s
```

（沒有變成 `cannot import name 'RETRY'`，代表 Phase 29 已經是兩條 retrier 的版本，D-53 成立。）

**綠燈** 同一條指令 → `22 passed in 21.18s`

**commit** `e418f8b feat(infra): feedback-review ASL 與每日排程`

---

## 4. 測試方式與結果

### 4.1 本機

| 指令 | 結果 |
|---|---|
| `uv run pytest tests/unit/test_feedback_review_flow.py -q` | **22 passed** |
| `uv run pytest tests -q -W error` | **1454 passed / 37 skipped / 11 xfailed**（skipped 全是 `aws` marker；xfailed 全是 O6 未核定的 `xfail(strict=True)`） |
| `uv run ruff check src tests infra` | All checks passed! |
| `uv run mypy` | Success: no issues found in 49 source files |

### 4.2 真實 AWS

```bash
TKB_RUN_AWS_INTEGRATION=1 TKB_CONTENT_BUCKET=trainingkbdata-…-dsm5eixpwnuv \
AWS_REGION=us-east-1 AWS_DEFAULT_REGION=us-east-1 \
  uv run pytest tests/integration/test_feedback_review_state_machine.py -q
→ 6 passed in 21.86s
```

#### 部署（`cdk deploy TrainingKbApp --exclusively`）

```text
✅  TrainingKbApp
✨  Deployment time: 90.25s
Outputs:
TrainingKbApp.FeedbackReviewLogGroup = /aws/vendedlogs/states/training-kb-feedback-review
TrainingKbApp.FeedbackReviewScheduleName = training-kb-feedback-review-daily
TrainingKbApp.FeedbackReviewStateMachineArn = arn:aws:states:us-east-1:123456789012:stateMachine:training-kb-feedback-review
```

#### `describe-state-machine` 與 ASL 快照

```text
{"type": "STANDARD", "status": "ACTIVE", "name": "training-kb-feedback-review",
 "role": "arn:aws:iam::123456789012:role/TrainingKbApp-FeedbackReviewRoleD8C956D5-8ERNLw5TvbS4"}
resource: arn:aws:lambda:us-east-1:123456789012:function:training-kb-pipeline-task   ← 直接函式 ARN，無信封
部署定義 == 本地 v1.json 換掉 ${PipelineTaskFunctionArn}：True

snapshot key: stepfunctions/feedback-review/v1.json
bytes identical: True
```

#### 每日排程（`aws scheduler get-schedule`，`REV` Rule 1 的雲端證據）

```json
{
    "name": "training-kb-feedback-review-daily",
    "expr": "cron(30 0 * * ? *)",
    "tz": "UTC",
    "state": "ENABLED",
    "window": {"Mode": "OFF"},
    "input": "{\"mode\": \"formal\"}",
    "arn": "arn:aws:states:us-east-1:123456789012:stateMachine:training-kb-feedback-review",
    "role": "arn:aws:iam::123456789012:role/TrainingKbApp-ReviewSchedulerRole833E211B-8i5TELioPu7W"
}
```

#### 執行 A：`{"mode": "formal"}`，零弱教學目標 → **SUCCEEDED**（O5 BLOCKED 下唯一跑得到的路徑）

```text
arn:aws:states:us-east-1:123456789012:execution:training-kb-feedback-review:p48-succeeded-1789451097
{
    "status": "SUCCEEDED", "error": null, "cause": null,
    "output": "{\"operation_id\": \"op-feedback-review-demo-2026-09-15\", \"project_id\": \"demo\",
                \"mode\": \"formal\",
                \"target_version_ids\": [\"demo-p52-successor-20260915@v1\", \"demo-site-check@v1\",
                                         \"p42-import-20260915@v2\", \"p46-refine-20260914d1@v1\"],
                \"candidate_rule_ids\": [\"R-800fc62e\"], \"prepared_version_ids\": [],
                \"publish_request_ref\": null,
                \"result_ref\": \"operations/op-feedback-review-demo-2026-09-15/review-result.json\"}"
}
TaskStateEntered: ListTargets  EvaluateTargets  PrepareBatch  InspectBatch  CommitBatch
SucceedStateEntered: Succeeded
```

輸出的鍵剛好是 00A §7 的**八個欄位**，`prepared_version_ids == []`、
`publish_request_ref is null`，**零次 Bedrock**。前置（整合測試自己補的）：正式表裡那一個
既有 candidate 組先放一條 `[合成資料]` 佔位規則，`propose_candidate` 才會走「已存在就原樣
回傳、不打模型」那條（P47 §7.2 第 2 點）；測試結束把規則刪掉。

#### 執行 B：`{"mode": "staging"}` → **FAILED**（不需模型）

```text
arn:aws:states:us-east-1:123456789012:execution:training-kb-feedback-review:p48-staging-1789450837
{"status": "FAILED", "error": "PipelineFailed", "cause": "本次整批失敗，不發布任何新版本"}
types: ExecutionStarted TaskStateEntered LambdaFunctionScheduled LambdaFunctionStarted
       LambdaFunctionFailed TaskStateExited FailStateEntered ExecutionFailed
TaskStateEntered: ListTargets            ← 連 EvaluateTargets 都沒進去
errorType: PermanentError                ← 只失敗一次，沒有重試（PermanentError 不在 RETRY[0]）
cause: {"errorMessage": "未知的 review mode：'staging'（只接受 formal／demo）",
        "errorType": "PermanentError", "requestId": "f0311763-…"}
```

#### 執行 C：`{"mode": "formal"}`，有一個沒有規則的 candidate 組 → **FAILED（O5 BLOCKED 證據）**

```text
arn:aws:states:us-east-1:123456789012:execution:training-kb-feedback-review:p48-blocked-1789451104
{"status": "FAILED", "error": "PipelineFailed", "cause": "本次整批失敗，不發布任何新版本"}
TaskStateEntered: ListTargets  EvaluateTargets          ← 沒有走到發布那三個 Task
errorType: PermanentError
cause: {"errorMessage": "O5 尚未通過：generation_model_id 還沒有實測值",
        "errorType": "PermanentError", "requestId": "4f862a22-…",
        "stackTrace": ["… pipelines/common.py, line 200, in pipeline_task_handler …",
                       "… pipelines/feedback.py, line 1022, in feedback_review_handler …",
                       "… pipelines/feedback.py, line 863, in task_evaluate_targets
                            found.append(propose_can…"]}
TaskFailed 事件：[]     ← 直接函式 ARN 的失敗是 lambdaFunctionFailedEventDetails（P41 A2 再驗一次）
```

**這是 BLOCKED 證據，不是 bug，也不是通過**（`docs/plan/report/o5-20260915T030245Z.md`）。
（同一條路在整合測試之前先用手跑過一次：`p48-formal-blocked-1789450879`，
`errorMessage` 逐字相同。）

#### 合成資料清理核對（測試 teardown 之後）

```text
RULE items: []
FEEDBACK ids with p48 prefix: []
FEEDBACK total: 10
OPS# op-feedback-review-demo-2026-09-15: None
operations/op-feedback-review-demo-* 物件: []
site/ 物件：13 個，全部是 P52／P57 與更早留下的，**本 Phase 一個都沒有加**
stepfunctions/ 快照：feedback-review/v1.json、release-update/v1.json、ticket-analysis/v1.json
```

---

## 5. 遇到的問題與解法

1. **單元測試的器材：手寫假 `Repository` 行不通。** 整條流程會走到 `create_version`／
   `verify_version_complete`／`Publisher.prepare|inspect|commit`／`transact_write`／
   lease／`record_version`，手寫替身等於複製一份會漂移的 `Repository` 副本。改成比照
   `tests/unit/test_feedback_refine.py`（P46）與 `tests/unit/test_publisher_single.py`（P24）
   已在用的做法：moto 表＋bucket 上的**真** `Repository`，只加一個記錄 `accept` 順序的
   `OperationCoordinator` 子類。
   種子資料也走真的 `create_version`（含 `.md`／`.diff`／STEP 邊）再 `update_meta` 補
   `published_at`／`current_version`，`Publisher.inspect` 的「已保存步驟 vs 公開內容」
   才比對得出來。順序是 **TUTORIAL item 先建**：`create_version` 最後的
   `verify_version_complete` 會查它（第一次跑就踩到 `找不到 TUTORIAL item a`）。
2. **`mypy --strict` 擋下 `list[str]` 進 state。** `JSONValue` 的 list 分支是
   `list[JSONValue]`，而 list 是不變的，所以 `list[str]` 不是它的子型別。加了
   `_ids`／`_listed` 兩個小函式集中轉換，沒有在各個 Task 灑 `cast`。
3. **「第二篇檢查失敗」要挑對切點。** 一開始改 STEP 邊的文字，結果 `prepare` 自己就先丟
   `PublishError`（`render_version_page` 會核對已保存步驟與公開內容），證不到「檢查失敗時
   零篇公開」。改成**基底位移**（`TUTORIAL.current_version` 不再等於新版的 `supersedes`）：
   `prepare` 照樣把兩篇的 staging 都寫出來，只有 `inspect` 擋得住，這才是要證的形狀。
   動手腳的時機放在 `EvaluateTargets` 與 `PrepareBatch` 之間——雲端逐個 Task 呼叫，那正是
   真的會出現的空隙。
4. **P41 留給本 Phase 的測試。** `tests/unit/pipelines/test_ticket_flow.py::
   test_module_without_its_handler_is_a_permanent_error` 原本靠「`pipelines/feedback.py` 是
   controller 預建的空殼」成立，它的 docstring 就寫「這條測試會在 P48 落地
   `feedback_review_handler` 之後自然失效，那時由 P48 改掉」。改成 `monkeypatch.setitem`
   把對照表指到一個真的沒有那個屬性的入口，**要守的行為完全不變**（接不起來時
   `errorType` 要說得出原因）。這是本 Phase 動到的唯一一條別人的測試。
5. **雲端跑不到 happy path。** 正式表裡有一個既有的 candidate 組（P46 留下的 8 筆回饋），
   `propose_candidate` 一定要打模型。整合測試自己先把那一組的規則放進表（明示
   `[合成資料]`），流程就一次模型都不用叫，`SUCCEEDED` 才跑得出來；測試結束刪掉。
   反過來的那一條（執行 C）則自己種五筆**新的**同類回饋造出沒有規則的組，保證打到模型，
   留下 BLOCKED 證據。兩邊都自足，不依賴別的 Phase 留在表裡的資料。
6. **同波次併行。** `infra/training_kb_stack.py` 同時有 P42（`training-kb-import`）與 P52
   （`release-update`）在加東西，全程只用 Edit、只動自己的 `# ---- Phase 48 ----` 區段、
   `git add` 只加自己的路徑。P52 已經先把 `tests/unit/infra/test_ticket_asl.py` 與
   `test_analytics_stack.py` 的 `resource_count_is(..., 1)` 改成包含關係（他們的 commit 裡
   註明「Phase 52 代改」），所以本 Phase 加第三條 state machine 與第三個 log group 之後
   那兩支檔仍然綠燈，我沒有再動它們。

---

## 6. Phase 文件修改清單

`docs/plan/unfinish/48-Phase48-Feedback-Review排程流程.md`

- §7 的 15 個 Step checkbox 與 §11 的 13 個完成清單項目**全部勾選**（28 個）。
- 新增 **§12 實作結果（2026-09-14）**：7 條「本計畫選擇」與 3 條「未做／做不到（原因）」。

---

## 7. gate 狀態與給下一個 Phase 的介面清單

### 7.1 gate

| gate | 本 Phase 的結論 |
|---|---|
| **O2** | **PASS（P11），本 Phase 依賴它**：當日 `operation_id` 由「專案＋UTC 日期」決定，同一天重送落在同一筆 `OPS#`；`accept` 回 `duplicate` 時沿用既有紀錄。單元測試 `test_resend_does_not_call_the_model_or_allocate_a_new_version` 證了「不重打模型、不提第二條規則、不配新版號」。 |
| **O3** | **維持 FAIL。** 真實 AWS 上**沒有任何一次執行走到發布切點**（O5 先擋在 `EvaluateTargets`），所以「多篇一起公開」的雲端驗收到不了，照實記錄、**不宣稱通過、不放寬 F49**。整批語意在 moto／單元上成立（§3 Task 2 的逐篇 commit 實驗是反證）。`TKB_FAULT` 的兩個發布切點**沒有做**——`faults.maybe_fail` 還沒被插進 `publishing.py`（那是 P59 的 Task），同一組觀察在 P25 的 `tests/integration/test_batch_publish_cutpoints.py`（協定 A）已經有。 |
| **O5** | **維持 BLOCKED。** 原文見 §4 執行 C：`O5 尚未通過：generation_model_id 還沒有實測值` → `Catch` → `PipelineFailed`。沒有填任何猜測的 `TKB_GENERATION_MODEL_ID`、沒有 mock 掉雲端 Writer。 |
| O1／O4／O6／O7 | 不受影響。本 Phase 沒有動 sort key 語意、沒有算重開票窗口、沒有讀核定來源、沒有把 candidate 說成已驗證。 |

### 7.2 給 P49／P52／P57／P59／P60 的介面清單

| 名稱 | 值／位置 |
|---|---|
| state machine ARN | `arn:aws:states:us-east-1:123456789012:stateMachine:training-kb-feedback-review` |
| state machine log group | `/aws/vendedlogs/states/training-kb-feedback-review`（90 天） |
| 排程名稱 | `training-kb-feedback-review-daily`（`cron(30 0 * * ? *)`／`UTC`／`{"mode": "formal"}`） |
| Scheduler 角色 | `TrainingKbApp-ReviewSchedulerRole833E211B-8i5TELioPu7W`（只有這一條 machine 的 `states:StartExecution`） |
| ASL 本地檔／快照 | `infra/stepfunctions/feedback-review/v1.json` ／ S3 `stepfunctions/feedback-review/v1.json`（bytes 相同） |
| stack 屬性 | `self.feedback_review`、`self.feedback_review_logs`、`self.review_scheduler_role`、`self.daily_review` |
| stack 模組常數 | `REVIEW_MACHINE`、`REVIEW_ASL_PATH`、`REVIEW_SCHEDULE_NAME`、`REVIEW_SCHEDULE_EXPRESSION`、`REVIEW_SCHEDULE_TIMEZONE`、`REVIEW_SCHEDULE_INPUT` |
| CfnOutput | `FeedbackReviewStateMachineArn`、`FeedbackReviewLogGroup`、`FeedbackReviewScheduleName` |

`from training_kb.pipelines.feedback import ...`：

```python
FEEDBACK_REVIEW_PIPELINE: PipelineName = "feedback-review"
REVIEW_MODES: tuple[ReviewMode, ...] = ("formal", "demo")
FEEDBACK_REVIEW_TASKS: tuple[TaskFn, ...]        # 五個，順序固定

def review_operation_id(state: Mapping[str, Any], deps: Deps) -> str: ...
def task_list_targets(state: dict[str, JSONValue], deps: Deps) -> dict[str, JSONValue]: ...
def task_evaluate_targets(state: dict[str, JSONValue], deps: Deps) -> dict[str, JSONValue]: ...
def task_prepare_batch(state: dict[str, JSONValue], deps: Deps) -> dict[str, JSONValue]: ...
def task_inspect_batch(state: dict[str, JSONValue], deps: Deps) -> dict[str, JSONValue]: ...
def task_commit_batch(state: dict[str, JSONValue], deps: Deps) -> dict[str, JSONValue]: ...
def run_feedback_review(state: dict[str, JSONValue], deps: Deps) -> dict[str, JSONValue]: ...
def feedback_review_handler(event: dict[str, Any], context: object) -> dict[str, JSONValue]: ...
```

module-private、**不要跨模組 import**：`_review_mode`、`_review_targets`、`_ids`、`_listed`、
`_publisher`、`_prepared`、`_TASK_BY_NAME`、`_DEPS`。

呼叫端要知道的五件事：

1. **state 只有八個欄位**（00A §7）：`operation_id`、`project_id`、`mode`、
   `target_version_ids`、`candidate_rule_ids`、`prepared_version_ids`、
   `publish_request_ref`、`result_ref`。逐篇理由與全文一律在
   `operations/<operation_id>/` 底下的私有物件。
2. **input 只有 `mode`**（`project_id` 可選，**沒有** `scheduled_for`，D-61）。Demo 手動觸發
   用**同一條** state machine 改傳 `{"mode": "demo"}`，不另建第四條 pipeline。
3. **停止點是 `review-result.json` ＋ `operations.complete(...)`**。`published_version_ids`
   有值也**不代表** O3 已通過（那份 gate 的決策出口在維護者手上）。
4. **`_prepared` 每個 Task 重算一次** `Publisher.prepare`：同 operation 的 staging 是決定性
   的、只寫私有前綴。要換成「把 `PreparedPublish` 放進 state」會違反 00A §7 的八個欄位。
5. **P43 合併後要改兩處**：`select_weak_targets` 與 `task_evaluate_targets` 都還在用
   `DEFAULT_FEEDBACK_CATEGORIES`，P43 的 `approved_categories(repository)` 落地後兩處一起換
   （程式裡都標了註解）。

---

## 8. commits

| SHA | subject |
|---|---|
| `19ab65d` | feat(feedback): 組合每日回饋檢視的兩條獨立分支 |
| `ea9cde7` | feat(feedback): 整批 prepare/inspect/commit 與零部分發布 |
| `e418f8b` | feat(infra): feedback-review ASL 與每日排程 |
| `bcf4fc6` | docs(plan): Phase 48 完成勾選與實作報告 |

**併行 git 事故（要給 controller 知道）：** 本 Phase 對
`infra/training_kb_stack.py` 的 `# ---- Phase 48 ----` 兩段（模組層六個常數 ＋ 建構子裡的
五個 construct）**沒有**出現在 `e418f8b` 裡，而是被同波次 P43 的
`fe309bb feat(ingress): 匯入時判定並保存回饋類別` 一起帶走了（`git log -S REVIEW_SCHEDULE_NAME
-- infra/training_kb_stack.py` → `fe309bb`）。推測是那支 agent 用了 `git add -A`／`git commit -a`
把工作樹裡我還沒提交的共用檔一併掃進去。**內容在 HEAD 上是完整且正確的**（`git show
HEAD:infra/training_kb_stack.py` 有四處 `Phase 48` 標記，工作樹對該檔的 diff 是空的），
部署出去的也是這份程式（§4 的 outputs 與 `describe-state-machine` 可對）。依 R3.4 **沒有**
用 `git commit --amend`／`git reset`／`git rebase` 去搬動它——那會把別人的 commit 輾掉。

---

## 9. 未完成／疑慮／建議 controller 裁決

1. **`TKB_FAULT` 的兩個發布切點沒有做**（Phase 文件 §7 Task 3 Step 4 要求）。
   `faults.maybe_fail` 目前**沒有**被插進 `publishing.py`（全案只有 `faults.py` 自己定義
   它），五個切點的插入點是 **Phase 59** 的 Task，本 Phase 不預先插進別人的區段。建議
   controller 把「多篇整批的兩個切點」併進 P59 的驗收清單。**O3 維持 FAIL。**
2. **雲端 happy path 仍然拿不到**：唯一 `SUCCEEDED` 的那條是「零弱教學目標」（0 次
   Bedrock）。O5 解鎖後要重跑一次「兩篇都產生 `RefinePlan`」才補得上 §8.1 驗收矩陣第二列
   與「兩篇一起公開」的雲端證據。
3. **每日 00:30 的實際觸發沒有等**（只用 `get-schedule` 存證）。要證真的被觸發，得等隔天
   看 `list-executions` 有沒有一條由 Scheduler 啟動的執行；那條執行在 O5 BLOCKED 下**會是
   `FAILED`**（正式表裡只要有一個沒有規則的 candidate 組就會打模型），不要誤判成排程壞了。
4. **本 Phase 動到一條別人的測試**：`tests/unit/pipelines/test_ticket_flow.py::
   test_module_without_its_handler_is_a_permanent_error`（P41 的 docstring 指定由 P48 改）。
   改法見 §5 第 4 點，行為不變。
5. **`test_resend_reuses_prepared_artifacts` 換了斷言**（Phase 文件 §12 本計畫選擇 3）。
   附帶發現一件值得記錄的事：**同一天重送已經成功的 review，第二次不會再產生任何待發布
   版本**——`current_version` 已經前進、`_guard` 會判 `no_new_evidence`（F23）。這是 P46 的
   既有契約，不是本 Phase 的 bug，但「重送＝沿用同一批 staging」這個說法只在**第一次失敗
   而且還沒 commit** 的情況下成立。建議 controller 在 00A §14.2 的措辭上註記一下。
6. **`project_id` 只影響 `operation_id`，不影響掃描範圍**：`_review_targets` 與
   `select_weak_targets` 都掃全表的 TUTORIAL，沒有依 `project_id` 過濾（既有函式的行為，
   本 Phase 沒有改）。多專案時要回頭看這一點。
7. **合成資料**：整合測試自己種、自己刪（§4 有清理核對原文）。留在正式表裡的只有別的
   Phase 的資料；本 Phase 一個 `site/` 物件都沒有新增。
8. **commit 歸屬有偏差**（見 §8 的併行 git 事故）：`infra/training_kb_stack.py` 的 Phase 48
   區段落在 P43 的 `fe309bb`，不在本 Phase 的 `e418f8b`。程式內容正確、已部署，但 reviewer
   看 `e418f8b` 的 diff 會看不到 stack 那一段，要一起看 `fe309bb`。建議 controller 提醒同波次
   的 agent **只 `git add` 自己的檔案路徑**（COMMON.md R3.3）。
