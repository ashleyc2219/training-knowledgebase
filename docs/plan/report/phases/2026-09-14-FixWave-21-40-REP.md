# Phase 21–40 Review 修正波次報告（2026-09-14）

實作者：單一 dev agent（唯一動 `src/`／`tests/` 的 agent）。分支 `backend`，未 push。
全程未動任何 `docs/` 檔案（文件整合由另一個 agent 負責）。

共 9 個 commit，每項必修／便宜修正各自獨立；每項一律先加會失敗的測試（RED，並確認是
**正確理由**的紅燈），再做最小實作（GREEN），最後跑 ruff／mypy。

---

## 0. 基準與最終 gate

| | 基準（開工前） | 最終 |
|---|---|---|
| `uv run pytest tests -q -W error` | 898 passed / 23 skipped / 11 xfailed | **924 passed / 23 skipped / 11 xfailed**（76.53s，零 warning） |
| `uv run ruff check src tests infra` | All checks passed! | **All checks passed!** |
| `uv run mypy src infra` | Success: no issues found in 37 source files | **Success: no issues found in 37 source files** |

新增 26 條測試、零回歸；xfail 仍然是 11 條（O6 gate，沒有變多也沒有變少）。
`grep -rn "# type: ignore" src/ infra/` 只命中 `content.py:415` 的**說明文字**（那行是
docstring 裡「本專案不用 `# type: ignore`」這句話），程式碼一個消音都沒有。

---

## A1 整批發布先寫 pending-promote 再重渲染

commit `6e92ba8` `fix(content): 整批發布先寫 pending-promote 再重渲染`

### 改了什麼
`src/training_kb/publishing.py` `Publisher._publish_site`：把 `self._record_pending_promote(prepared)`
從 N 次 `_restage` **之後**移到方法的第一個動作（`operation_id` 取值之後）。
五步順序的 docstring 一併改寫，並寫明理由：payload 只由 `prepared.version_ids` 決定（確定性）、
只寫私有前綴、`if_none_match=False` 允許同 operation 重送覆寫，所以提前寫是零風險；
排在重新渲染之後的話，「交易成功、第一次 `_restage` 就中斷」這個切點會讓 DynamoDB 已經切換、
`operations/<op>/pending-promote.json` 卻不存在，而父 operation 依 D-59 不持有版號，
P59 復原整批就沒有任何輸入。

### 測試
`tests/unit/test_publisher_batch.py::test_pending_promote_list_exists_when_the_first_restage_breaks`
——`monkeypatch` 把 `publisher._restage` 換成丟 `TransientError`，斷言
`commit` 丟 `PublishError`（切點 `a3_after_first_site_before_second`）、
`pending-promote.json` 已存在且列出**全部** 4 個 site key、公開前綴一個物件都沒有。

### 指令與輸出
```
$ uv run pytest tests/unit/test_publisher_batch.py -q -W error -k first_restage_breaks   # RED
>       pending = json.loads(repo.objects[PENDING_KEY])
E       KeyError: 'operations/op-review-0914/pending-promote.json'
1 failed, 25 deselected

$ uv run pytest tests/unit/test_publisher_batch.py tests/unit/test_publisher_single.py \
      tests/integration/test_batch_publish_cutpoints.py tests/integration/test_publish_cutpoints.py -q -W error
54 passed in 39.97s
$ uv run ruff check src tests infra   -> All checks passed!
$ uv run mypy src infra               -> Success: no issues found in 37 source files
```

---

## A-minor 1 索引頁改走公開寫入的未發布標記守門

commit `70093ff` `fix(content): 索引頁改走公開寫入的未發布標記守門`

### 改了什麼
`publishing.py`：
- `_put_public_object(...)` 加上 keyword-only 的 `if_none_match: bool = True`，成為
  **寫進 `site/` 的唯一出口**。`True` 維持原本的一次性條件寫入（撞鍵比對 bytes）；
  `False` 是索引頁的覆寫語意，但一樣先過 `UNPUBLISHED_MARKER` 守門。
- `Publisher._put_index` 不再直接 `repository.put_object(_public_key(...), ...)`，
  改呼叫 `_put_public_object(..., if_none_match=False)`。覆寫語意完全不變。

### 測試
`tests/unit/test_publisher_batch.py::test_index_pages_go_through_the_same_unpublished_marker_guard`
——把 `publisher._renderer.render_tutorial_index` 換成回 `data-published="false"` 的字串，
斷言 `commit` 丟 `PublishError("未發布標記不得進公開前綴…")`，且 `site/` 底下沒有任何 `index.html`。

### 指令與輸出
```
$ uv run pytest tests/unit/test_publisher_batch.py -q -W error -k unpublished_marker_guard  # RED
E       Failed: DID NOT RAISE <class 'training_kb.errors.PublishError'>
1 failed, 26 deselected

$ uv run pytest tests/unit/test_publisher_batch.py tests/unit/test_publisher_single.py -q -W error
43 passed in 27.58s
$ uv run pytest tests/integration/test_batch_publish_cutpoints.py tests/integration/test_publish_cutpoints.py \
      tests/integration/test_private_artifacts.py tests/unit/test_o3_observation.py -q -W error
26 passed in 11.72s
$ uv run ruff check src tests infra   -> All checks passed!
$ uv run mypy src infra               -> Success: no issues found in 37 source files
```

---

## A2 教學索引改用 P27 的版本查詢

commit `794724c` `refactor(content): 教學索引改用 P27 的版本查詢`

### 改了什麼
`publishing.py`：
- `_write_tutorial_index` 改成
  `[v for v in self._repository.list_versions_of_tutorial(slug) if v.published_at is not None][::-1]`
  ——P27 已用 `version_sort_key` 排成升序，反轉後與原本「版號大的在前」逐字相同，
  本模組不再自己掃一次 `VERSION`、也不再自己排一次序（兩份排序邏輯遲早分岔）。
- 過期的 `_models("VERSION"/"TUTORIAL", model)` 只剩 `TUTORIAL` 一個用途 →
  改名 `_tutorials()`，刪掉「Phase 27 的 `list_versions_of_tutorial` 還不存在」那段過期 docstring，
  換成「站台索引要看到全部 `TUTORIAL`，P27 沒有對應的固定查詢，所以仍走 `scan_entity`」。
- `_write_site_index` 改呼叫 `self._tutorials()`。

### 測試
`tests/unit/test_publisher_single.py::test_tutorial_index_is_built_from_the_phase_27_version_query`
——`monkeypatch` 在 `repo.list_versions_of_tutorial` 上掛 spy，斷言 `commit` 之後
`calls == [SLUG]`（真的走 P27 查詢），並斷言輸出順序仍是 v2 在 v1 之前。
既有索引測試（`test_commit_writes_public_page_and_diff_copy_and_indexes` 等）全部維持綠燈。

### 指令與輸出
```
$ uv run pytest tests/unit/test_publisher_single.py -q -W error -k phase_27_version_query  # RED
>       assert calls == [SLUG]
E       AssertionError: assert [] == ['prepare-meeting']
1 failed, 16 deselected

$ uv run pytest tests/unit/test_publisher_single.py tests/unit/test_publisher_batch.py -q -W error
44 passed in 31.44s
$ uv run pytest tests/integration/test_batch_publish_cutpoints.py tests/integration/test_publish_cutpoints.py \
      tests/unit/test_site_renderer.py tests/unit/test_retired_page.py -q -W error
23 passed in 13.33s
$ uv run ruff check src tests infra   -> All checks passed!
$ uv run mypy src infra               -> Success: no issues found in 37 source files
```

---

## A3 移除 P27 落地前的 active 教學 fallback

commit `3f9cbe2` `refactor(ticket): 移除 P27 落地前的 active 教學 fallback`

### 改了什麼
`src/training_kb/pipelines/ticket.py`：刪掉 `@runtime_checkable class _ActiveTutorialFinder(Protocol)`
與 `_active_tutorial` 裡的基表 fallback，`_active_tutorial` 現在只剩
`return repository.find_active_tutorial_for_feature(feature_id)`；
`from typing import Any, Literal, Protocol, runtime_checkable` → `from typing import Any, Literal`
（ruff F401）。`tests/unit/pipelines/test_ticket_decide.py` 的 `FakeRepository` 已經有那個方法，
不必改測試。

### 測試
`tests/unit/pipelines/test_ticket_decide.py::test_active_lookup_has_no_silent_fallback`
——`monkeypatch.delattr(FakeRepository, "find_active_tutorial_for_feature")` 之後，
`decide_ticket_action` 必須丟 `AttributeError` 而不是安靜地改走另一套判準。
這條測試同時鎖住 F12：備援在查詢缺席時會回「沒有 active 教學」，把 KEEP 判成 CREATE，
同一個功能就被建第二篇。

### 指令與輸出
```
$ uv run pytest tests/unit/pipelines/test_ticket_decide.py -q -W error -k no_silent_fallback  # RED
E       Failed: DID NOT RAISE <class 'AttributeError'>      （fallback 安靜地回了 KEEP）
1 failed, 11 deselected

$ uv run pytest tests/unit/pipelines -q -W error
100 passed in 0.22s
$ uv run ruff check src tests infra   -> All checks passed!
$ uv run mypy src infra               -> Success: no issues found in 37 source files
```

---

## A5 教學索引頁顯示退役提示與後繼

commit `f1ef75a` `feat(content): 教學索引頁顯示退役提示與後繼`

### 改了什麼（controller 裁決出口 c）
`src/training_kb/site.py`：
- 新增模組私有 `_retired_block(tutorial)`：`status != retired` 回空字串，否則回
  `<p class="retired">RETIRED_NOTICE</p>` + `_successor_line(tutorial.successor)`。
- `render_version_page` 尾端由原本的兩行 inline 改成 `page + _retired_block(tutorial) + "</article>"`
  ——**輸出逐字不變**（既有 P26 版本頁測試全綠）。
- `render_tutorial_index` 在版本清單之後、`</article>` 之前插入同一個 `_retired_block`。
- **三個 renderer 的簽名一個都沒動**（00A §6.7）：索引 renderer 本來就收得到 `Tutorial`，
  而 `Tutorial.successor` 就是 P26 `resolve_successor` 四項檢查的**結果**
  （不合法時 `retire_tutorial` 讓它保持 `None`），所以不需要任何新參數。
- 連結與版本頁完全一致：印 slug、`href="../<successor>/index.html"`。兩個頁面都在
  `site/tutorials/<slug>/` 這一層，所以同一個相對路徑在兩邊都解析得對。
- 版本頁不變；退役後由 P52 呼叫 `Publisher` 重寫該索引頁（本波不做）。

### 測試
`tests/unit/test_retired_page.py`（4 條新）：
- retired + 合法 successor → 有 `class="retired"`、`RETIRED_NOTICE`、`<a href="../prepare-meeting/index.html">`，且不含 `release:r_88`；
- active（即使身上有 successor）→ 兩行都不輸出；
- retired + `successor=None`（四項檢查沒過的結果）→ 有提示、沒有連結，也沒有任何 `index.html`；
- successor slug 含 `"`／`<` → 一樣 `html.escape`。

`tests/unit/test_retire_tutorial.py`（1 條新，parametrize 兩例）：
`test_tutorial_index_links_only_the_successor_that_passed_the_four_checks`
——真的跑 `retire_tutorial(..., successor="not-exists" | "prepare-meeting")`，再把回傳的
`Tutorial` 餵給 `SiteRenderer().render_tutorial_index`：提示一定在，連結只有合法那次在。
這條把「四項檢查的結果」與「公開頁看得到什麼」接在一起。

### 指令與輸出
```
$ uv run pytest tests/unit/test_retired_page.py -q -W error   # RED
FAILED ...::test_tutorial_index_of_a_retired_tutorial_shows_notice_and_successor
FAILED ...::test_tutorial_index_omits_the_link_when_the_successor_was_rejected
FAILED ...::test_tutorial_index_escapes_the_successor_slug
3 failed, 6 passed

$ uv run pytest tests/unit/test_retired_page.py tests/unit/test_site_renderer.py -q -W error
15 passed in 0.05s
$ uv run pytest tests/unit/test_retire_tutorial.py -q -W error
20 passed in 0.06s
$ uv run pytest tests/unit/test_publisher_single.py tests/unit/test_publisher_batch.py \
      tests/unit/test_o3_observation.py tests/integration/test_publish_cutpoints.py \
      tests/integration/test_batch_publish_cutpoints.py tests/integration/test_retired_feedback_rejected.py -q -W error
69 passed in 28.83s
$ uv run ruff check src tests infra   -> All checks passed!
$ uv run mypy src infra               -> Success: no issues found in 37 source files
```

---

## A-minor 2 transact_write 套用 Decimal codec

commit `d7185d8` `fix(data): transact_write 套用 Decimal codec`

### 改了什麼
`src/training_kb/repository.py` `Repository.transact_write`：
`TransactItems=list(items)` → `TransactItems=[_encode(dict(item)) for item in items]`。
`_encode` 會遞迴進 list／tuple／dict，所以 item 裡任何位置的 `float` 都變成 `Decimal(str(...))`，
與 `put_meta`／`update_meta`／`put_meta_item` 同一套 codec。docstring 補上理由。

### 測試
`tests/integration/test_repository_meta.py::test_transact_write_values_survive_the_decimal_codec`
（moto）——送一個帶 `:score = 0.5` 與 `:tags = [0.25, 1]` 的 `Update` action，
斷言 `transact_write` 回 `None`（全部成功）且讀回來的值等值。

### 指令與輸出
```
$ uv run pytest tests/integration/test_repository_meta.py -q -W error -k transact_write_values   # RED
E       TypeError: Float types are not supported. Use Decimal types instead.
1 failed, 15 deselected

$ uv run pytest tests/integration/test_repository_meta.py -q -W error
16 passed in 4.68s
$ uv run pytest tests/integration -q -W error
192 passed, 23 skipped, 11 xfailed in 49.67s
$ uv run ruff check src tests infra   -> All checks passed!
$ uv run mypy src infra               -> Success: no issues found in 37 source files
```

---

## B2 子 Release 序號支援 JSONPath 並收斂例外

commit `9d08d5b` `fix(rote): 子 Release 序號支援 JSONPath 並收斂例外`

### 改了什麼
`src/training_kb/rote.py`：新增模組私有 `_sub_release_index(step: ProcStep, run: _Run) -> int`，
`_sub_releases` 的 `recorded = int(steps[position].args.get(SUB_RELEASE_INDEX, "1"))`
改成 `recorded = _sub_release_index(steps[position], run)`。

helper 的取值規則與 `_Run.run` **同一套**：字面值走 `is_index_literal`（唯一允許的字面值例外），
否則 `run.value(raw)`（`resolve_jsonpath`，它自己只丟 `PermanentError`）。
結果不是 `int`（`bool` 另外擋掉——它是 `int` 的子類，放行的話 `True` 會變成序號 1）
就丟 `PermanentError("子 Release 序號不是整數：<path> -> <型別名>")`。
訊息只放 path 與型別名，**不放解析出來的值**（那可能是事件真值，00A §3.8）。

原本的裸 `ValueError` 會讓 `normalize_all` 的 `except PermanentError` 接不到，
PROC 不累計 `fail_count`、不回退 Agent，handler 直接 5xx。

### 測試
`tests/unit/rote/test_fallback.py` 三條，**直接測 helper**（不依賴被 O6 xfail 擋住的整合測試）：
- 字面值 `"1"`／`"7"`／沒帶參數（預設 1）→ 1／7／1；
- JSONPath `$steps[0].k`（輸出 `{"k": 3}`）→ 3；
- JSONPath 取到 `"3"`／`1.5`／`True`／`None` → `PermanentError("子 Release 序號不是整數")`。

### 指令與輸出
```
$ uv run pytest tests/unit/rote/test_fallback.py -q -W error   # RED
E   ImportError: cannot import name '_sub_release_index' from 'training_kb.rote'

$ uv run pytest tests/unit/rote -q -W error
66 passed in 0.07s
$ uv run pytest tests/unit/rote tests/integration/test_release_extraction.py \
      tests/integration/test_rote_commit.py tests/integration/test_rote_lookup.py \
      tests/unit/test_ingress_validation.py tests/unit/test_ingress_acceptance.py -q -W error
156 passed, 4 xfailed in 4.09s
$ uv run ruff check src tests infra   -> All checks passed!
$ uv run mypy src infra               -> Success: no issues found in 37 source files
```

---

## B-minor 1 重放失敗紀錄寫入失敗不影響回退

commit `e7ae6af` `fix(rote): 重放失敗紀錄寫入失敗不影響回退`

### 改了什麼
`src/training_kb/rote.py`：`normalize_all` 的 `except PermanentError` 分支改呼叫新的
`Rote._record_replay_failure(selected)`，那個方法**只把 `_persist` 包起來**：
```python
try:
    self._persist(on_replay_failure(proc, self.deps.now()))
except CoordinationError:
    _log.warning("replay failure not recorded, proc=%s", proc.signature)
```
`CoordinationError` 只代表 revision CAS 輸了（這次的 `fail_count` 沒記到），
讓它取代原本的 `PermanentError` 往外飛的話，本次事件直接失敗，
ING Rule 17 的「當次回退 Agent」一次都跑不到。**其他例外不吞**。
同時在 `rote.py` 加了模組 logger（`_log = logging.getLogger(__name__)`），
訊息只有簽名（一個雜湊值），不含事件內容（00A §3.8）。

### 測試
`tests/unit/rote/test_fallback.py` 兩條：
- `test_failed_replay_record_write_still_falls_back_to_the_agent`：`monkeypatch` 讓
  `_persist` 丟 `CoordinationError` → 仍然回 `route == "agent_after_replay_failure"`、
  `validated is True`、`agent_calls == 1`；
- `test_other_persist_failures_are_not_swallowed`：`_persist` 丟 `TransientError` →
  原樣往外拋，`agent_calls == 0`（證明「只吞 `CoordinationError`」）。

### 指令與輸出
```
$ uv run pytest tests/unit/rote/test_fallback.py -q -W error   # RED
E       training_kb.errors.CoordinationError: PROC 在寫入前有人同時改過
1 failed, 18 passed

$ uv run pytest tests/unit/rote -q -W error
68 passed in 0.07s
$ uv run pytest tests/unit tests/integration/test_rote_commit.py tests/integration/test_rote_lookup.py -q -W error
739 passed, 1 xfailed in 29.10s
$ uv run ruff check src tests infra   -> All checks passed!
$ uv run mypy src infra               -> Success: no issues found in 37 source files
```

---

## B3 續跑補啟動後仍提交 PROC 成功

commit `e329285` `fix(ingress): 續跑補啟動後仍提交 PROC 成功`

### 改了什麼（選項 a）
`src/training_kb/ingress.py`：
- `_accept(...)` 的回傳型別改成 `tuple[Acceptance, bool]`，bool 是
  「**進來之前**就已經完整啟動過」——那是函式開頭 `status == "duplicate" and execution_arn`
  的觀察，早退那條回 `True`，走完啟動那條回 `False`。
- 新增模組私有 `_accept_detailed(obj: Ticket | Release, *, deadline) -> tuple[Acceptance, bool]`，
  把原本散在 `accept_ticket`／`accept_release`／`accept_normalized` 的型別分派收在一處。
- **三個公開 `accept_*` 的簽名完全不變**，各自 `return _accept_detailed(...)[0]`。
- `normalize_then_accept` 改用那個 bool：`if not already_started: rote.commit_success(...)`。

原本的 `if not (acceptance.status == "duplicate" and arn)` 用的是 start **之後**的狀態，
而續跑（ledger 已 accepted／duplicate、`execution_arn` 仍是 `None`，本次補完啟動）
結束時與純重送長得一模一樣，所以會被誤判成純重送而跳過 `commit_success`，這條 PROC 永遠學不起來。
去重的權威仍然是 `record_proc_sample`（docstring 已寫明）。

### 測試
`tests/integration/test_rote_commit.py::test_resumed_acceptance_still_commits_the_proc_success`
——先用真的 `OperationCoordinator.accept` 寫一筆 `OPS#op-ticket-t_gh-acme-copilot-128`
（斷言 `execution_arn` 是 `None`），再跑 `normalize_then_accept`：
`status == "duplicate"` 但本次真的啟動了（`deps.started` 有一筆），
且 `get_proc(signature).success_count == 1`（PROC 建立了）。
接著再跑一次（純重送，arn 已在）→ `deps.commit_calls` 仍是 1、`success_count` 仍是 1。
既有的 `test_normalize_then_accept_runs_the_whole_fixed_order`（純重送不重複提交）維持綠燈。

### 指令與輸出
```
$ uv run pytest tests/integration/test_rote_commit.py -q -W error -k resumed_acceptance   # RED
>       assert stored is not None and stored.success_count == 1
E       assert (None is not None)          （PROC 根本沒被建立）
1 failed, 9 deselected

$ uv run pytest tests/integration/test_rote_commit.py tests/unit/test_ingress_acceptance.py \
      tests/integration/test_github_webhook_handler.py -q -W error
58 passed, 1 xfailed in 3.16s
$ uv run ruff check src tests infra   -> All checks passed!
$ uv run mypy src infra               -> Success: no issues found in 37 source files
```

---

## B-minor 2 驗證時間檔損壞一律 PermanentError

commit `a4f3173` `fix(analytics): 驗證時間檔損壞一律 PermanentError`

### 改了什麼
`src/training_kb/analytics/status_writer.py` `load_validated_at`：
把 `body.decode("utf-8")`、`json.loads(...)` 與「必須是物件」的檢查全部移進 `try`，
`except ValueError` 改成 `except (ValueError, UnicodeDecodeError)`。
`PermanentError` 不是 `ValueError` 的子類，所以 `try` 裡那個「不是物件」的 raise
不會被自己的 `except` 再包一層（docstring 已寫明）。

### 測試
`tests/unit/pipelines/test_ticket_create_v1.py::test_load_validated_at_turns_a_corrupt_file_into_a_permanent_error`
——parametrize 四種壞檔：`b"{not json"`、`b"\xff\xfe"`（壞 UTF-8）、`b""`、`b'"R-007"'`，
全部必須是 `PermanentError`。

### 指令與輸出
```
$ uv run pytest tests/unit/pipelines/test_ticket_create_v1.py -q -W error -k corrupt_file   # RED
E       json.decoder.JSONDecodeError: Expecting value: line 1 column 1 (char 0)
3 failed, 1 passed, 19 deselected

$ uv run pytest tests/unit/pipelines -q -W error
104 passed in 0.12s
$ uv run ruff check src tests infra   -> All checks passed!
$ uv run mypy src infra               -> Success: no issues found in 37 source files
```

---

## 最終三個 gate（全部跑完之後重跑一次）

```
$ uv run pytest tests -q -W error
924 passed, 23 skipped, 11 xfailed in 76.53s (0:01:16)

$ uv run ruff check src tests infra
All checks passed!

$ uv run mypy src infra
Success: no issues found in 37 source files
```

## commits（依序）

```
6e92ba8 fix(content): 整批發布先寫 pending-promote 再重渲染
70093ff fix(content): 索引頁改走公開寫入的未發布標記守門
794724c refactor(content): 教學索引改用 P27 的版本查詢
3f9cbe2 refactor(ticket): 移除 P27 落地前的 active 教學 fallback
f1ef75a feat(content): 教學索引頁顯示退役提示與後繼
d7185d8 fix(data): transact_write 套用 Decimal codec
9d08d5b fix(rote): 子 Release 序號支援 JSONPath 並收斂例外
e7ae6af fix(rote): 重放失敗紀錄寫入失敗不影響回退
e329285 fix(ingress): 續跑補啟動後仍提交 PROC 成功
a4f3173 fix(analytics): 驗證時間檔損壞一律 PermanentError
```
（10 行是因為 A-minor 1 獨立成一個 commit；A1 與它是同一個檔的兩件事，刻意分開。）

## 疑慮與未做的事

1. **A5 只做了 renderer 端。** 退役之後真正讓讀者看到提示，要等 P52 在退役後呼叫
   `Publisher` 重寫該教學的索引頁——本波依裁決不做。現在退役一篇教學而不重新發布任何版本，
   公開站上的索引頁仍是舊的 bytes。
2. **O3 仍然是 FAIL。** A1 只是把整批切點的**復原輸入**補齊（`pending-promote.json` 一定先存在），
   partial 本身沒有被消除，也沒有放寬 F49。moto 全綠不是 O3 的驗收證據。
3. **B2 的整合路徑仍被 O6 擋住。** F14 兩筆子 Release 的端到端測試是
   `xfail(strict=True, reason="O6 尚未核定 github.com/pull_request")`，所以 helper 用單元測試
   直接驗證；O6 核定後那條整合測試會自動變成真正的覆蓋。
4. **A3 之後 `_active_tutorial` 對 repository 的要求變硬。** 任何自備 fake repository 的
   未來測試都必須提供 `find_active_tutorial_for_feature`；這是刻意的（缺它要大聲失敗）。
5. `rote.py` 因為 B-minor 1 多了一個模組 logger，這是本專案第二處 `logging`
   （第一處是 `handlers/github_webhook.py`）。訊息只有 PROC 簽名，不含事件內容。
