| 檢查 | 狀態 | 掃描範圍 |
|---|---|---|
| `檢查 三次不同事件走同一條 PROC，確認 success_count 至少為 3（ING Rule 7）` | **pass** | DynamoDB `training_kb` 的 PROC item（真實帳號唯讀 scan） |
| `執行 對 webhook 各送一次正確簽名與一次錯誤簽名的 curl，預期一個接受、一個拒絕且錯誤簽名不寫入任何業務物件` | **pass** | `POST https://examplewebhookurlid0000000000000.lambda-url.us-east-1.on.aws/`（自簽的合成請求；真實 GitHub 未設定 webhook） |
| `執行 對 Titan 與 Claude 各做一次小量試呼叫，記下 model／inference profile 與回應摘要` | **not_run** | Bedrock（Titan／Claude）小量試呼叫 |
| `確認 重送同一事件不新增版本也不新增樣本（O2 永久去重）` | **pass** | 同一份 payload 換 delivery 重送，比對 operation_id |

### `檢查 三次不同事件走同一條 PROC，確認 success_count 至少為 3（ING Rule 7）` 的發現

- PROC#d1ad3cfd19a24c4d success_count=3

### `執行 對 webhook 各送一次正確簽名與一次錯誤簽名的 curl，預期一個接受、一個拒絕且錯誤簽名不寫入任何業務物件` 的發現

- 正確簽名 http=200 body={"operation_ids":["op-ticket-t_gh-acme-copilot-128"],"operation_id":"op-ticket-t_gh-acme-copilot-128","ok":true}
- 錯誤簽名 http=200 body={"operation_id":null,"ok":false,"message":"GitHub 簽名不符","fields":["X-Hub-Signature-256"]}

### `執行 對 Titan 與 Claude 各做一次小量試呼叫，記下 model／inference profile 與回應摘要` 的發現

- O5 BLOCKED：`ValidationException: Operation not allowed`，見 docs/plan/report/o5-20260915T030245Z.md；本次刻意不對真實 Bedrock 發請求

### `確認 重送同一事件不新增版本也不新增樣本（O2 永久去重）` 的發現

- 第二次 http=200 body={"operation_ids":["op-ticket-t_gh-acme-copilot-128"],"operation_id":"op-ticket-t_gh-acme-copilot-128","ok":true}
- 與第一次逐字相同 = True

文件 parser 成功不等於 runtime 通過。
