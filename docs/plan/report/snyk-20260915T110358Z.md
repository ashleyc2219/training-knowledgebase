# 依賴與金鑰掃描紀錄 snyk-20260915T110358Z

金鑰掃描（Snyk Secrets）與依賴掃描（Snyk Open Source）是**兩種能力**，本檔分開記錄；金鑰結論見 `check_secrets`。
| 工具 | CLI 版本 | 指令 | 掃描範圍 | 退出碼 | 退出碼意義 |
|---|---|---|---|---|---|
| snyk | `1.1307.2` | `snyk test` | 專案根目錄的依賴宣告（pyproject.toml／uv.lock） | 3 | 沒有偵測到支援的專案（不是通過） |
| pip-audit（**非 Snyk**） | `以 `uv run --with pip-audit` 取得最新版（未固定）` | `uv run --with pip-audit pip-audit` | 已安裝的直接與間接依賴（PyPI advisory 資料庫） | 1 | 掃描完成，有發現問題（不是掃描失敗） |

- `snyk` 已涵蓋範圍：（無）
- `snyk` 未涵蓋範圍：code、dependencies、secrets
- `pip-audit` 已涵蓋範圍：dependencies
- `pip-audit` 未涵蓋範圍：code、secrets

金鑰掃描的結論只能來自 `check_secrets`，不得由任何依賴掃描代替。

## 實際輸出（原文節錄）

```text
$ snyk test

 FATAL   No supported files found (SNYK-CLI-0008)
                                                                                                                                                      
           Could not detect supported target files in ~/AWS-Hackathon.                                                                
           Please see our documentation for supported languages and target files: https://snyk.co/udVgQ and make sure you are in the right directory. 

Docs:    https://docs.snyk.io/scan-with-snyk/error-catalog#snyk-cli-0008 
Tip:     Try snyk doctor: `snyk <command> -d 2>&1 | snyk doctor --stdin` 
                                                                         
ID:      urn:snyk:interaction:1cd1a06e-b511-4e6e-88df-97620d8bab40 


$ uv run --with pip-audit pip-audit
Name   Version ID              Fix Versions
------ ------- --------------- ------------
pytest 8.4.2   PYSEC-2026-1845 9.0.3
pytest 8.4.2   PYSEC-2026-1845 9.0.3

Name        Skip Reason
----------- --------------------------------------------------------------------------
training-kb Dependency not found on PyPI and could not be audited: training-kb (0.1.0)
Found 2 known vulnerabilities in 1 package

```
